# Correzioni

Il registro di cosa è stato corretto, diviso per modulo. Una riga per
correzione: **data, cosa, commit**.

**Prima di correggere qualcosa, cercalo qui.** Se risulta già corretto,
fermati e dillo, con data e commit — non rifarlo. Una correzione che si
ripresenta non è una correzione: è il sintomo che la prima non ha tenuto, o
che è stata scavalcata da una modifica successiva. Rifarla in silenzio
nasconde il problema vero.

Ogni correzione va aggiunta qui **nello stesso commit** che la applica, come
i test (regola 23). Una correzione registrata dopo è una correzione che, nel
frattempo, qualcun altro ha già rifatto.

Il *perché* di una scelta non sta qui: sta nel commento accanto al codice e
nel messaggio di commit. Qui sta il *cosa*, in una riga, per poterlo
ritrovare.

Nella colonna **Commit** sta l'hash quando la correzione è di un commit
precedente. Le righe aggiunte *insieme* alla correzione che descrivono portano
invece il **titolo** del commit: un commit non può contenere il proprio hash, e
mettercelo con una modifica successiva significherebbe scriverlo fuori dal
commit che lo giustifica. Il titolo si ritrova con `git log --grep`.

---

## Condiviso (mappa, esiti, configurazione)

| Data | Cosa | Commit |
|---|---|---|
| 2026-09-19 | **PASSO B: lo stato di sessione esce in `app/sessione.js`, e con lui I QUATTRO PEZZI BLOCCATI.** `itemText`, `recordPendingMastery`, `recordMultipleChoiceResult`, `buildMultipleChoiceOptions` erano registrati da giorni come bloccati dallo stato di sessione: **non erano bloccati da lui, erano lui.** Dentro il file anche l'aritmetica della mastery (`LEVELS`, `nextLevel`, `prevLevel`, `applyMasteryResult`) — misurato: `applyMasteryResult` ha **un solo chiamante**, `recordPendingMastery`, quindi sono un blocco unico. ⚠️ **LA RIGA CHE PERMETTE A B DI VENIRE PRIMA DI C: l'episodio si RICEVE, non si calcola.** `currentEpisode` nasceva da `EPISODES[CONFIG.episodioCorrente]`, cioè dal **catalogo**, che è ancora in `index.html`. Se `sessione.js` lo calcolasse, chiederebbe `EPISODES` all'insù e il conto salirebbe invece di scendere. Ora `index.html` calcola e **consegna** con `BI.impostaEpisodioCorrente`, gemello di `impostaValoriCorrenti` che esisteva già dal 21-quater: *non è un meccanismo nuovo, è la sua simmetria.* **È la stessa distinzione che il 18 settembre aveva già deciso cosa poteva entrare in `ui-condivisa`:** `fillTemplate` riceve episodio e valori come parametri, `itemText` andava a prenderli — *uno li riceve, l'altro se li va a cercare, e chi se li va a cercare deve stare dove sono.* ⚠️⚠️ **LE DIPENDENZE ALL'INSÙ SCENDONO DA 10 A 3, ED È IL NUMERO PER CUI QUESTA SERIE ESISTE.** ⚠️ **E LA MISURA HA CORRETTO LA PREVISIONE, per la quarta volta in due giorni:** avevo dichiarato **10 → 1**. I tre che restano sono `dati.js` (`applyEpisodeDialogue`, scelta dichiarata), `mappa.js` (`EPISODES`, `migrateCustomizeSeenToModuleProgress`) e `personalizza.js` (`ID_PERSONALIZZA`, `ensureEpisodeSlotFields`): **sono tutti CATALOGO, non stato di sessione**, e escono col passo C. *Avevo contato solo `applyEpisodeDialogue` e dimenticato gli altri quattro nomi: la previsione era ottimista di due file.* **Il caso più diverso era `app/personalizza.js`, l'unico dei sette lettori che SCRIVE** nei valori (`BI.valoriCorrenti()[key] = …`) invece di leggerli: `valoriCorrenti()` torna **l'oggetto e non una copia**, e con una copia avrebbe smesso di funzionare **in silenzio** — nessun errore, solo un nome che non si salva. Guidato: `test_batch10` 23/23, `test_new_features` 30/30, `test_mastery_al_gesto` 26/26. **Nove dichiarazioni `DIPENDE DA` corrette verso la misura** (i nove file che ora chiedono a `sessione.js` invece che a `index.html`). ⚠️ **E un rosso da seguire che dice una cosa sola:** `[B] itemText e' rimasta in index.html` di `test_ui_condivisa_estratta.js` è diventata `itemText sta in app/sessione.js, non qui`. **L'invariante non è cambiato** — `ui-condivisa` non tocca lo stato di sessione — è cambiata l'altra metà della prova. ⚠️ **E il baseline dell'avvio ha il DIFF PIÙ LUNGO della serie per il cambiamento più piccolo:** il tag entra **in mezzo** (dopo `ui-condivisa`, per gli alias) e non in fondo come i sei moduli, quindi **nove file slittano di uno**. Moduli registrati e pulizie invariati: `sessione.js` non registra niente, tiene lo stato. Versione dei tag a `20260919m`. | titolo: `Passo B: lo stato di sessione e i quattro bloccati` |
| 2026-09-19 | **PASSO A: la vista morta `view-pronunciation` è uscita.** 244 righe di JS, 43 di markup, **quattro listener** e l'apparato `SpeechRecognition` che serviva solo a lei: `startPronunciationExercise` aveva **una sola occorrenza in tutto il repository, la propria definizione**. ⚠️ **NON è stata una cancellazione, ed è il punto del passo: il listener globale della Regola Azione Critica stava IN MEZZO**, vivo, ed è quello che interroga `BI.dgAudioProtected`. È stato **separato** e ha adesso un blocco suo che dice perché sta lì da solo. ⚠️ **E LA MISURA HA FERMATO DUE CANCELLAZIONI SBAGLIATE, tutte e due nel verso «sembra morto e non lo è».** ① **`.phrase` è VIVA:** la usa `#vc-feedback` di **Voice**, che ci scrive dentro uno `<span class="correct|similar|wrong">` per parola — e stava sotto un'intestazione CSS che diceva `/* ---------- pronunciation view ---------- */`. *Un'intestazione che nomina una vista fa sembrare di quella vista tutto ciò che sta sotto, anche quello che un altro modulo ha riusato.* Presa **misurando i lettori invece di leggendo il titolo**; l'intestazione è stata riscritta col motivo accanto. ② `.transcript-box` e `.status-text` idem, entrambe di Voice. Morte e tolte davvero: `.phrase span.new`, `.legend`, `.dot*`, `.edit-custom-link`, `.listen-row`, `#speak-btn.speaking`, `.controls`, `.mic-btn`, `@keyframes pulse`, `.top-row` (e i **quattro commenti** che la usavano come punto di riferimento sono stati riportati a `.header-2row`). ⚠️ **E UN GUASTO VERO, trovato guidando e non dai test: `BI.views` elencava ancora `pronunciation`.** L'elemento non c'era più, quindi la mappa conteneva un `null` e **al primo `showView` l'app moriva** con `Cannot read properties of null`. **Il boot riusciva lo stesso** — `view-onboarding` si accendeva — quindi aprendo la pagina non si vedeva niente: si vedeva al **primo cambio di schermata**. *Chi toglie una vista deve togliere la sua riga da quell'elenco: un dato che nomina un elemento sparito non dà un elenco più corto, dà un `null` che aspetta.* Il motivo sta adesso accanto a `showView`. **`LEVEL_CLASS` e la classe `new` sono uscite con lei**, come deciso: erano l'unico posto che disegnava la voce **mai incontrata**, e il loro unico lettore era `renderPhrase`. ⚠️ **`tests/BASELINE-LISTENER.txt` CAMBIA PER LA PRIMA VOLTA: 107 → 103**, e in otto giri del 21-quater non era cambiato di una riga. I quattro erano **scritti prima di lanciare** (`back-home`, `edit-custom`, `mic-btn`, `speak-btn`), e sono esattamente quattro: *un baseline che cala senza un numero scritto prima non distingue una pulizia da una perdita.* Corretti anche `tests/listener-census.js` (il caso `#edit-custom` resta al passato: è quello che ha insegnato la regola) e il punto 24 di `tests/README.md`. **E si perde una cosa da sapere:** `#edit-custom` era **l'unico punto oltre alla mappa** che apriva un modulo chiedendolo al registro — da oggi `BI.moduli` ha un solo lettore che apre. Versione dei tag a `20260919k`. | titolo: `Passo A: la vista morta esce, il listener globale si separa` |
| 2026-09-19 | **IL SESTO E ULTIMO MODULO ESCE: `app/dialogo.js`. I SEI SONO TUTTI FUORI.** 736 righe, 430 di codice, un componente per i tre profili. **Verso `index.html` chiede DUE nomi, e sono soli accessori** (`episodioCorrente`, `valoriCorrenti`): **nessuno dei quattro bloccati dallo stato di sessione serve qui** — il Dialogo non fa quiz a scelta multipla e non scrive mastery. Porta via la **terza e ultima pulizia** (`dgClearAllTimers`): da qui `stopAllModuleActivity` non ne registra più nessuna da `index.html`. **Ed è l'unico che espone un nome**, `dgAudioProtected` — l'eccezione alla regola 16: il listener globale in cattura vive in `index.html` e deve chiedere «questo audio è protetto?», ma la risposta la sa solo il Dialogo (dipende dal profilo e dalla vista attiva). Il chiamante è guardato, e **non per prudenza: se quel file non è caricato la risposta GIUSTA è «nessuna protezione»**, perché senza Dialogo aperto non c'è nessun countdown da sfasare. ⚠️ **E IL GUASTO CHE CI È STATO, perché vale più dell'estrazione: `openEpisodeMap is not defined`, e `buchi.js` aveva detto solo prosa.** Il Dialogo si apriva sulla **schermata d'errore**. La causa: `openEpisodeMap` è passata a un listener come **riferimento** — `addEventListener('click', openEpisodeMap)` — e **un riferimento non ha la parentesi**, quindi la ricerca dello strumento, che cerca `nome(`, non poteva vederlo. *È la stessa forma del lookbehind di ieri: non un errore che si annuncia, ma un controllo che dice «non c'è niente» (regola 37)* — con l'aggravante che il guasto compariva **solo aprendo quel modulo**, perché il nome viveva dentro un listener. **`buchi.js` ha adesso una SECONDA ricerca** (`BUCHI-RIF`) per gli identificatori nudi in posizione di argomento, stretta di proposito per non stampare ogni variabile del file; passata su tutti e otto i moduli, l'unico rumore che resta è la **prosa dentro i commenti di blocco**, cioè il limite già dichiarato in testa allo strumento. ⚠️ **E il colpevole non l'ha trovato lo strumento: l'ha trovato una SONDA nel `.catch` di `openModuleFromMap`**, che ingoiava l'errore e mostrava la schermata — la stessa famiglia del primo modulo. Trovata anche una seconda cosa: il `})(window.BI);` era finito **sulla stessa riga** del commento finale, quindi **commentato**, e il file non era JavaScript valido; `node --check` su tutti e venti i file di `app/` dopo la correzione. **La misura ha smentito una mia dichiarazione, per la terza volta oggi:** avevo scritto in testa al file «non chiede niente a `index.html`», e `dipendenze.js` conta i due accessori. *Scrivere zero sarebbe stato un numero più bello di quello vero.* Censimento: file con dipendenze 11 → 12, all'insù 9 → 10, a parsing 9 → 10; script dell'avvio 20 → 21, **e ancora una volta l'unica riga del baseline che cambia**. Versione dei tag a `20260919j`. | titolo: `Il sesto e ultimo modulo esce: app/dialogo.js` |
| 2026-09-19 | **Il quinto modulo esce: `app/voice.js`** — 912 righe, 471 di codice: il più grande dei sei. Voice Practice e Voice Check sono un componente solo dietro due kind. **Resta il Dialogo.** ⚠️ **È L'UNICO MODULO CON UN APPARATO, e l'apparato esce con lui:** `vcRecognition` nasce a tempo di **parsing**, dentro il file, e lì prende `onresult`/`onend`/`onerror` — assegnazioni di proprietà, non `addEventListener`, quindi il censimento dei listener non le ha mai viste. Funziona perché è un **singleton**; chi un giorno caricasse questo file su richiesta deve sapere che qui non nasce solo del codice, nasce un oggetto che parla col microfono. ⚠️ **E porta via la SECONDA pulizia** (`vcResetRecording`), come previsto dalla riga registrata con Speed Match. **Un solo nome all'insù, ed è ancora uno dei quattro:** `recordPendingMastery` — lo stesso di Flash Card. **I due riferimenti allo stato di sessione non sono nomi nuovi:** `currentEpisode` e `currentValues` si chiedono con `BI.episodioCorrente()` e `BI.valoriCorrenti()`, gli accessori che `app/mappa.js` usa già — *sono funzioni, non alias, e lì sta tutta la differenza: un alias fotograferebbe il valore di adesso, una funzione legge quello di quando la chiami.* **Cinque moduli, nessuna ragione diversa dalla prima.** ⚠️ **E LA MISURA HA SMENTITO UNA MIA PREVISIONE, per la seconda volta oggi — stavolta nel verso buono.** Avevo dichiarato due diff nel baseline dell'avvio (ordine dei moduli e delle pulizie), *«attesi come per speedmatch»*. **Non cambiano:** `voicePractice`/`voiceCoach` restano 10 e 11, `vcResetRecording` resta pulizia 4, e **l'unica riga che cambia è il conto degli script, 19 → 20**. Il motivo è che `voice.js` prende in fila il posto che la sua regione aveva rispetto ai file già usciti. *Uno spostamento che non muove niente è esattamente quello che l'intestazione di `BASELINE-AVVIO.txt` chiede a un'estrazione fatta bene* — e la dichiarazione è stata corretta verso la misura, dentro lo strumento, dove torna a ogni rigenerazione. `buchi.js` prima della suite: **undici voci, tutte prosa dentro i commenti**, lette una per una. Censimento: file con dipendenze 10 → 11, all'insù 8 → 9, a parsing 8 → 9 (Voice ha una ragione in più di tutti per la seconda fila, e non è un alias: è l'apparato). Guidato da sette file, tutti verdi, compreso l'avviso microfono (31/31) e il comportamento dell'audio (13/13). Versione dei tag a `20260919i`. | titolo: `Il quinto modulo esce: app/voice.js` |
| 2026-09-19 | **L'etichetta sopra la bolla del Dialogo adesso ha un'asserzione** — blocco `[Etichetta]` di `tests/test_dialogo_extra.js`, 4 righe, **20 → 24**. Chiude la riga registrata lo stesso giorno: `speakerLabel` ha **due** lettori e solo Meet the Story ne guardava l'effetto visibile (col guasto, `test_story_modules` **92/93** e questo file **20/20**). **Scritta PRIMA del Dialogo e non insieme**, perché è la condizione dichiarata di quel passo: arrivarci senza la riga significava rifare il 18. ⚠️ **Le etichette attese non sono scritte nel test:** vengono da `speakerLabels` del file episodio e i nomi scelti da `slotValues()`, cioè dalle due sorgenti che l'app stessa legge — *un elenco a mano dentro un test è un campione, e un campione sceglie chi non guardare.* **Le tre righe non sono ridondanti, ed è la diagnosi (⓪-octies):** la prima cade su tutti e tre i guasti, la seconda e la terza dicono **quale** delle due decisioni didattiche della regola 4 si è rotta. **Vista fallire su tutti e tre, e ognuno nomina le bolle:** l'id tecnico (9), il solo mestiere — «Hostess» invece di «Hostess al gate» (3), il nome scelto — «Marco» invece di «Papà» (2). ⚠️ **E DUE COSE TROVATE SCRIVENDOLA, nessuna delle due prevista.** ① **`.dg-name` è SORELLA di `.dg-bubble`, non figlia:** la bolla è un `<button>` e il nome sta fuori, così non finisce nel testo del bersaglio del tocco — il contenitore è `.dg-item`. Il primo giro cercava `.dg-bubble .dg-name` e trovava **zero con nove nomi a schermo**. ② **E l'ha detto il `try` con la diagnosi, non il timeout:** senza, il file **moriva** con un `TimeoutError` (⓪-septies) e il messaggio sarebbe stato «ho aspettato troppo»; con la diagnosi ha stampato `{item, bolle: 9, nomi: 9}`, cioè la causa vera al primo colpo. *È la stessa forma di `attendi.sh`, che dice quale dei due guasti ha davanti.* Aggiunta anche la riga `// PROTEGGE:` in testa al file, che non ce l'aveva (regola 32). `app/ui-condivisa.js` è tornato identico dopo le tre falsificazioni (diff vuoto, verificato). | titolo: `L'etichetta sopra la bolla del Dialogo ha un'asserzione` |
| 2026-09-19 | **Il quarto modulo esce: `app/speedmatch.js`** — 419 righe, e il **primo che porta via un TIMER**. ⚠️ **I nomi all'insù verso `index.html` sono TRE, e sono gli STESSI TRE di Match** (`itemText`, `recordMultipleChoiceResult`, `buildMultipleChoiceOptions`). **Il numero ha smesso di salire, e dice la stessa cosa che diceva salendo:** Speed Match e Match Practice sono lo stesso meccanismo a scelta multipla con un timer in mezzo, quindi toccano la **stessa fetta** dei quattro pezzi bloccati dallo stato di sessione. *Nessuna ragione diversa, per il secondo passo di fila.* ⚠️ **E UNA COSA NUOVA CHE I TRE MODULI PRECEDENTI NON AVEVANO: è la prima estrazione che porta via una PULIZIA.** `srPulizia` chiama `BI.registraPulizia` a tempo di **parsing**, quindi si registra quando il file viene letto — e nel baseline dell'avvio risale da `pulizia 5` a `pulizia 3`. Vuol dire che il magazzino di `stopAllModuleActivity` **non ha più un contenuto fisso: ha quello dei file caricati**. Oggi non cambia niente (i diciotto tag si caricano tutti al boot), ma dal giorno in cui un modulo si carica su richiesta, una pulizia non registrata è una pulizia che non gira. Registrato in `decisioni.md` con la sua condizione. Il diff del baseline è quindi **doppio e atteso**: la pulizia che risale e i due `kind` registrati insieme (`speedMatchEngIta`, `speedMatchItaEng`), come per match. `buchi.js` prima della suite: i tre `BI.` di proposito, `tick` che è una funzione annidata, e cinque parole di **prosa dentro i commenti** (`screen`, `Help`, `attempts`, `call`, `pool`) — lette una per una invece che credute. ⚠️ **E la misura ha corretto la mia dichiarazione, per la terza volta in questa serie:** avevo scritto `DIPENDE DA` senza `spazio.js [parsing]`, e `dipendenze.js` l'ha detto. **Corretta la dichiarazione verso la misura, mai il contrario.** Censimento seguito: file con dipendenze 9 → 10, all'insù 7 → 8, a parsing 7 → 8; script dell'avvio 18 → 19. Versione dei tag a `20260919h`. Guidato dalla suite che già lo guida fino in fondo (`test_batch19`, 40/40: countdown, barra del tempo, «Non lo so», tocco giusto, mastery, zero errori JS). | titolo: `Il quarto modulo esce: app/speedmatch.js` |
| 2026-09-19 | **Il terzo modulo esce: `app/match.js`** — 28 pezzi, 352 righe, e registra **due** kind con la stessa `open` (`matchEngIta`, `matchItaEng`). ⚠️ **E con lui arriva la risposta alla domanda che i due precedenti avevano lasciato aperta.** Chiede a `index.html` **tre** nomi (`itemText`, `recordMultipleChoiceResult`, `buildMultipleChoiceOptions`) — uno al primo modulo, due al secondo, tre al terzo — **ma sono sempre gli STESSI quattro**, quelli registrati come bloccati dallo **stato di sessione**. *Il numero sale perché ogni modulo ne tocca una fetta diversa, non perché nasca una ragione nuova.* Era esattamente questo il numero da guardare al terzo modulo: **nessuna ragione diversa è comparsa**, e i quattro spariscono insieme col loro passo. Nessun guasto: `buchi.js` — quello **corretto ieri col lookbehind** — ha dato solo prosa, e il modulo è stato guidato nel browser fino alla risposta (4 opzioni, disabilitate dopo la scelta, zero errori JS). Baseline dell'avvio 17 → 18 script. Versione dei tag a `20260919g`. ⚠️ **E un rosso in suite che NON è stato causato da questo passo, ma reso VISIBILE da lui:** `test_new_features.js` leggeva `window.APP_CONFIG` subito dopo il ricaricamento innescato dal «Reset» del Pannello Admin, e trovava `undefined`. L'attesa era su «il documento è stato sostituito» — vero all'**inizio** del nuovo documento, mentre fra quel momento e `APP_CONFIG` ci sono **diciotto script** da caricare. *La corsa c'era già: ogni tag nuovo la allarga.* È la regola 19 vista dal verso del **numero di file** invece che da quello della macchina lenta — e va saputo, perché restano tre moduli da estrarre e ognuno aggiunge un tag. Approdo corretto: `.view.is-active`, cioè `boot()` ha scelto una vista, e non è nessuna delle due cose che quelle righe leggono (regola 44). *Primo tentativo `#name-input`, sbagliato: quel profilo è già registrato e il login non compare.* | titolo: `Il terzo modulo esce: app/match.js` |
| 2026-09-19 | **Il secondo modulo esce: `app/flashcard.js`** — 36 pezzi, 395 righe, il più piccolo fra i cinque rimasti. **Due nomi all'insù verso `index.html` (`itemText`, `recordPendingMastery`), e la ragione è UNA SOLA:** lo stato di sessione. Sono due dei quattro registrati come bloccati da quello, ed escono tutti insieme col suo passo. *Quello da guardare al terzo modulo non è se il numero sale: è se compare una ragione DIVERSA.* Entrambi chiamati come `BI.nome(...)`, mai aliasati — la lezione del primo modulo, ora vietata da `[E]`. ⚠️ **DUE STRUMENTI CORRETTI, ED È LA PARTE CHE VALE PIÙ DELL'ESTRAZIONE.** ① **`tests/tools/buchi.js` diceva ZERO su un file che moriva.** Flash Card usciva con `staParlando is not defined` al primo tocco su una carta, e lo strumento non lo vedeva: la sua ricerca **consumava il delimitatore** (`[^.\w$'"`](nome)\s*\(`), quindi su `if (staParlando())` il primo match era `if (` e si portava via la parentesi del nome. **Invisibile ogni chiamata subito dentro una parentesi appena aperta** — `if (`, `while (`, `return (`, `&& (`. Corretto con un lookbehind. *È la forma peggiore della regola 37: non un errore che si annuncia, ma un controllo che dice «non c'è niente».* ② **`tests/tools/dipendenze.js`, sesta correzione: la profondità si conta fino al NOME, non fino a inizio riga.** Su una funzione scritta tutta su una riga (`function fcFrontText() { return BI.itemText(...); }`) la graffa non era ancora contata, e la chiamata risultava **a tempo di parsing** — falso, e falso nel verso che spaventa: «sta per succedere prima che `index.html` esista», su codice che gira solo quando lo si chiama. *Una misura sbagliata nel verso allarmante è peggio di una sbagliata nel verso comodo: si finisce per correggere il codice buono per far tacere lo strumento.* Baseline dell'avvio rigenerato (16 → 17 script); cambiano anche l'ordine dei moduli registrati e delle pulizie, **ed è un diff atteso dello spostamento**, non un cambio di comportamento: `openModuleByKind` cerca per chiave e le pulizie girano tutte. Versione dei tag a `20260919f`. | titolo: `Il secondo modulo esce: app/flashcard.js` |
| 2026-09-19 | **IL PRIMO MODULO È FUORI: `app/repeataloud.js`.** Quattro pezzi, 117 righe. **Scelto per primo perché è il più piccolo dei sei** (gli altri stanno fra 262 e 462 righe): se la meccanica dell'estrazione ha un difetto, si vede qui al costo minore. **E ne ha avuto uno.** ⚠️ **UN ALIAS SU UN NOME CHE NON ESISTE ANCORA, e vale per i cinque moduli che mancano.** Il file aliasava `itemText` — l'unica cosa che chiede a `index.html` — ma **lo script inline di `index.html` è l'ULTIMO**: sta in fondo a `<body>`, dopo tutti i tag `<script src>`. L'alias congelava **`undefined` per sempre**. Il modulo si apriva col corpo fermo su «Caricamento...» e un `TypeError: itemText is not a function` **ingoiato dal `.catch` del caricamento**: nessun errore in pagina, solo un modulo che non finisce mai. *È la stessa famiglia dell'alias su una variabile riassegnata (`istruzioniInMemoria`, 18 settembre): là il valore cambiava dopo, qui non esiste ancora prima. Un alias fotografa, e il problema è sempre QUANDO.* ⚠️ **E NON L'HA VISTO NESSUNO DEI DUE CONTROLLI:** `buchi.js` guarda **se** un nome è definito o aliasato, non **quando** arriva; e `test_batch16` è **MORTO** con un `TimeoutError` invece di fallire (⓪-septies, terza volta oggi). L'ha trovato una sonda messa a mano nel `.catch`. Nasce il blocco `[E]` di `test_dipendenze_dichiarate.js`, che **vieta** l'alias su un nome di `index.html`; falsificato rimettendolo, e cadono **due** righe. **Le dipendenze all'insù passano da 4 a 5, e la ragione è UNA SOLA: `itemText`**, che lega lo stato di sessione. *È il numero da guardare al secondo modulo: se al terzo è ancora uno e ancora `itemText`, il passo dello stato di sessione è in ritardo; se ne aggiunge altri, la ragione non c'era.* **E una cosa imparata sul tag:** questo file NON tocca nessun nodo mentre viene letto — i suoi sette listener si agganciano dentro la `open` — eppure sta nella seconda fila lo stesso, per i suoi venti alias e per `registraModulo` al primo livello. **Le ragioni della seconda fila sono due, non una.** Versione dei tag a `20260919e`. | titolo: `Il primo modulo esce: app/repeataloud.js` |
| 2026-09-19 | **`attendi-ci.sh` stampava un numero falso accanto a un verdetto vero.** Ha scritto «completata con success **(1s)**» su una corsa durata **585 secondi**: il verdetto veniva dall'API ed era giusto, il numero era **il tempo che lo script aveva aspettato** — e l'attesa era partita a corsa già finita. *È la forma della regola 37 nella sua versione più scivolosa: non un errore che si annuncia, ma una misura che non misura appoggiata a un risultato corretto.* ⚠️ **E non è un dettaglio da poco: la regola 38 dice che le durate della CI «stanno nell'elenco delle corse su GitHub» proprio perché un numero scritto altrove invecchia — e questo strumento stava producendo numeri che non erano nemmeno veri al momento in cui li scriveva.* Adesso la durata si legge da `run_started_at`/`updated_at` e l'attesa è dichiarata a parte: `(corsa: 585s, attesa: 1s)`. Nasce il blocco `[I]` di `tests/test_attendi_ci.js` (31 → 33), che finge una corsa **già finita** da 300 secondi — così l'attesa è zero e la confusione fra i due numeri cadrebbe subito; falsificato rimettendo il numero vecchio, e il rosso stampa `success (0s)`. **Trovato per caso**, guardando un numero che non tornava dopo una corsa normale. | titolo: `Il numero della CI e' quello della corsa, non della mia attesa` |
| 2026-09-19 | **Il pre-passo ai sei moduli: quattro pezzi condivisi escono PRIMA che un modulo se li porti dietro.** `renderChoiceBox` (Voice, Flash Card, Match, Dialogo), `startTimerBar` e `freezeTimerBar` (Speed Match, Dialogo), `DIRECTION_LABEL` (Flash Card, Speed Match, Match) passano in `app/ui-condivisa.js`. **Escono adesso e non col primo modulo** per la ragione che questa serie ha già pagato due volte: *un confine sbagliato che non produce nessun rosso è quello che si eredita.* ⚠️ **E QUATTRO RESTANO FUORI DI PROPOSITO, dichiarati invece che forzati:** `itemText`, `recordPendingMastery`, `recordMultipleChoiceResult`, `buildMultipleChoiceOptions` sono condivisi allo stesso modo ma legano lo **stato di sessione** — entrerebbero rompendo l'unica cosa che lo strato promette. Escono col loro passo. ⚠️ **LA COSA CHE CONTA DI PIÙ, ed è una falsificazione che NON ha morso: la barra del tempo non aveva NESSUNA rete.** Durata portata a `9999s` → `test_batch9`, `test_batch16`, `test_dialogo_extra` **tutti verdi**; tolta perfino l'esposizione di `startTimerBar` → `test_batch9` ancora **22/22**. *È esattamente la forma del 18 — un pezzo raggiungibile e un comportamento non guidato — e stavolta la riga è nata col pezzo invece che dopo il danno.* Nasce il blocco `[G]` di `test_ui_condivisa_estratta.js` (4 asserzioni), che guida i quattro pezzi su un elemento vero **dentro una chiamata sincrona**; falsificato su tre guasti separati. ⚠️ **E una correzione dentro la correzione:** al primo giro, togliendo l'esposizione, il file **moriva** invece di fallire (la `evaluate` solleva, il processo esce senza riepilogo) — ⑰-septies, la seconda volta oggi. Col `try` adesso escono quattro righe rosse che **nominano la causa**. Versione dei tag a `20260919d`. | titolo: `Il pre-passo ai moduli: i quattro pezzi condivisi movibili` |
| 2026-09-19 | **Il terzo gruppo passa in `app/ui-condivisa.js`, e con lui i sette pezzi sono tutti fuori.** `introDismissPref` (otto chiamanti: sette moduli e la mappa), `dialogueLineAlign` e `speakerLabel` (Dialogo e Meet the Story). **Nessun sintomo lo chiedeva**, ed è il punto: erano pezzi condivisi che stavano in un modulo **per posizione**, e i sei moduli ancora da estrarre li avrebbero ereditati così — cioè uno strato che chiede a un modulo. *Un confine sbagliato che non produce nessun rosso è quello che si eredita.* Nessuno dei tre lega lo stato di sessione: `speakerLabel` riceve l'episodio come **parametro**, come `fillTemplate` — ed è la ragione per cui entra mentre `itemText` resta fuori. `buchi.js` prima della suite: ZERO, e l'unico «buco» segnalato (`contenuto`) è **prosa dentro un commento di blocco**, il limite dichiarato dello strumento, letto riga per riga invece che creduto. ⚠️ **Falsificato con un guasto di COMPORTAMENTO, non di raggiungibilità** — `speakerLabel` che torna l'id tecnico invece dell'etichetta: `test_story_modules` cade (**92/93**). **Ma `test_dialogo_extra` e `test_batch16` restano VERDI**, cioè dei due lettori ne è guardato **uno solo**. Registrato in `decisioni.md` con la sua condizione: è esattamente il tipo di buco che il 18 è costato caro. Versione dei tag a `20260919c`. | titolo: `Il terzo gruppo passa a ui-condivisa` |
| 2026-09-19 | **Il Blocco Ascolto passa in `app/ui-condivisa.js` — secondo dei tre gruppi, e il SOSPETTATO PRINCIPALE delle due regressioni del 18.** `renderListenBlock` e `speakListenBlock`, **da sole**. Quel giro le spostò insieme ad altri cinque pezzi, la suite era verde tre volte, e su Pages il tocco rispondeva con ~3 secondi di ritardo. **Stavolta hanno sotto `tests/test_comportamento_audio.js`, che le GUIDA invece di trovarle.** **Sta qui per mestiere:** `renderRateButtons` — la metà del componente — vive in questo file dal giorno in cui lo strato è nato; tenere separata l'altra metà era «sta vicino» scambiato per «è suo», al contrario. Due esposizioni in meno su `index.html`: le dipendenze all'insù restano 3, ma due nomi condivisi in meno le attraversano. **Falsificato sul guasto che riproduce il sintomo vero** — `speakListenBlock` rinviata di un tick → **9/13**, e cadono le quattro righe di `[A]`. *Il punto non è che il test sappia morire: è che continui a mordere **dopo** lo spostamento, cioè esattamente dove il 18 non c'era nessuno a guardare.* `tests/tools/buchi.js` passato prima della suite: ZERO e ZERO. ⚠️ **E una riga che avevo scritto io, falsa, smentita dalla misura:** avevo dichiarato `icon` e `toggleSpeak` come dipendenze **a tempo di chiamata**, perché servono quando un pulsante si disegna o si tocca. È vero di *dove si usano* e falso di *come arrivano*: un alias in cima all'IIFE prende il valore **mentre il file viene letto**, quindi sono a tempo di **parsing**. L'ha detto `test_dipendenze_dichiarate.js`, e la dichiarazione è stata **corretta verso la misura**, non la misura verso la dichiarazione. *È la ragione per cui quel file esiste: una riga `DIPENDE DA` si sbaglia come qualunque altra.* **Versione dei tag alzata a `20260919b`**, com'è obbligatorio da oggi quando cambiano `index.html` o un file sotto `app/`. ⚠️ **Un rosso in suite, ed è una DECISIONE SEGUITA** (⑰-undecies): `test_blocco_ascolto.js` contava il markup **nel solo `index.html`** e trovava **zero** — cioè il messaggio «qualcuno ha ricopiato il componente» su un albero in cui nessuno l'aveva ricopiato. *Una domanda giusta posta al file sbagliato dà una risposta sbagliata con la faccia di una giusta.* L'invariante non è cambiato — «il markup ha UNA sola sorgente» — è cambiato dove quella sorgente vive: la ricerca ora copre `index.html` **più tutti i file sotto `app/`**, ed è **più forte** di prima, perché l'ottava copia scritta a mano cade ovunque la si metta. Falsificata aggiungendo una copia in `app/storycards.js` → 18/19, e dice 2. | titolo: `Il Blocco Ascolto passa a ui-condivisa` |
| 2026-09-19 | **⚠️ LA CAUSA DEL 18 NON ERA NEL CODICE: ERA LA CACHE DEL BROWSER. I quattordici tag portano una versione.** Il pannello Help risultava morto su Pages mentre qui funzionava; `Ctrl+Shift+R` lo ha riparato. **Finché l'app era un file solo «hai la pagina vecchia» era tutto-o-niente; da tre commit sono quindici file con cache indipendenti**, e un browser può tenere `index.html` nuovo e `app/ui-condivisa.js` vecchio — servendo un'app che nessuno ha mai scritto. ⚠️ **È il primo sospettato per il guasto del 18**, quello archiviato come «le quattro funzioni sono identiche carattere per carattere, il diff pulito, la causa non trovata»: *un insieme misto di file vecchi e nuovi produce esattamente quella descrizione*, e spiega perché né il container né la CI l'hanno mai visto — lì i file si leggono sempre tutti insieme dal disco. **Il problema l'abbiamo prodotto noi con lo spacchettamento, e nessuno poteva saperlo prima.** Chiuso con `?v=<versione>` **uguale su tutti e quattordici**: così un `index.html` vecchio chiede i quattordici all'indirizzo vecchio e ottiene un insieme **coerente** — è il caso MISTO che va reso impossibile, non quello vecchio. **E non è un promemoria:** `tests/test_versione_cache.js` (12 asserzioni) verifica che la portino uguale, e la CI **rifiuta** un push che cambia `index.html` o un file sotto `app/` senza alzarla (`tests/tools/versione-salita.js`, uscite 0/1/64). Nasce **`posizioneTag()`** in `tests/strati.js`, perché otto punti cercavano `src="app/x.js"` con la virgoletta finale e davano −1 su tutti e quattordici. ⚠️ **E il test ha trovato un difetto nello strumento al primo giro:** guardava **sempre** il repository in cui vive, quindi le prove sul repo finto misuravano quello vero e tornavano verdi per il motivo sbagliato (⓪-nonies). Da lì `--repo`. ⚠️ **E UN SECONDO GIRO, che è la lezione della regola 41 per intero:** dopo aver corretto otto punti e lanciato undici test mirati, la suite è andata **rossa lo stesso** — `tests/tools/dipendenze.js` cercava `src="app\/([\w.-]+)"` e con il `?v=` **non trovava più niente**: grafo vuoto, 29 asserzioni che non partivano e un `TypeError` invece di un rosso che spiega. *La verifica per sottrazione non l'aveva presa perché cercavo `src="app` e lì la barra è **escaped**: `src="app\/`.* È **la forma ⑥ della regola 41** — il percorso che non esiste mai per intero — e l'unico modo di prenderla è cercare il **segmento**, non la stringa. Dopo la correzione il setaccio è stato rifatto su quattro forme (`src="app`, `app\\/`, `'app/' +`, `querySelectorAll('script')`) e i restanti sono tutti tolleranti o basati su `readdirSync`. | titolo: `La versione sui quattordici tag` |
| 2026-09-19 | **Il popup dei tentativi passa in `app/ui-condivisa.js` — primo dei tre gruppi in cui i sette pezzi sono stati divisi.** Viene **intero**: `openAttemptPopup`, `closeAttemptPopup`, lo stato `attemptPopupOnRetry`/`attemptPopupOnNext` e i **due listener** su «Riprova» e «Vai avanti» — *un pezzo non è solo le sue funzioni.* **Chiude il debito dichiarato in testa a `app/mappa.js`:** `stopAllModuleActivity` chiedeva `BI.closeAttemptPopup` alla regione di **Voice**, cioè uno STRATO che chiede a un MODULO, il verso che questa serie esiste per eliminare. Le dipendenze all'insù verso `index.html` scendono da 4 a 3. La frase di `app/mappa.js` che il commit rende falsa è corretta nello stesso commit. **Il gruppo è stato scelto per primo di proposito:** è l'unico dei tre che paga un debito, ed è il **meno sospettato** delle due regressioni di ieri — il microfono di Voice Practice non passa di qui, passa da `speakListenBlock`, che resta dov'è. **`tests/tools/buchi.js` passato PRIMA della suite: ZERO e ZERO.** Due rossi, **tutti e due decisioni seguite** (⓪-undecies): `[S]` chiedeva l'alias per `moduloDiAiutoAttivo` (dato, come per `istruzioniInMemoria`), e `[C]` contava quattro listener — **ora sei, col motivo accanto**: l'invariante «questo file aggancia listener a tempo di parsing, ed è la ragione della seconda fila» non è cambiato, sono cambiate le ragioni, da una a due. | titolo: `Il popup dei tentativi passa a ui-condivisa` |
| 2026-09-19 | **⚠️ UN GUASTO VIVO SU PAGES, e l'ha trovato `buchi.js` in due secondi: il pannello Help si apriva e moriva al primo pulsante.** `activeHelpModule` vive in `app/ui-condivisa.js` ed è **riassegnata**, ma i **tre** punti che la leggono sono listener rimasti in `index.html`: «Promemoria», «Indietro» e l'invio di una richiesta d'aiuto davano tutti e tre `activeHelpModule is not defined`. **Verificato guidando l'app, non leggendo il codice.** Chiuso con **`moduloDiAiutoAttivo()`** — una funzione, non un alias, perché un alias su un nome riassegnato ne congela il valore iniziale (`null`) per sempre: è la stessa famiglia di `istruzioniInMemoria` del 2026-09-18. ⚠️ **E l'origine è scomoda: la correzione esisteva già, dentro il commit dei sette pezzi, e il `git revert` di ieri l'ha portata via insieme al resto.** *Un revert non annulla solo ciò che si voleva annullare: annulla tutto quello che stava nello stesso commit* — ed è una ragione in più per fare passi piccoli. ⚠️ **Nessuna delle 1458 asserzioni lo vedeva**, e non per caso: `[C]` contava i listener, `[D]` apriva l'app, `[E]` guidava Escape — **nessuna premeva un pulsante DENTRO il menu**. Nasce `[F]` di `test_ui_condivisa_estratta.js`, che lo preme e legge l'effetto nella stessa chiamata sincrona del click; falsificato rimettendo il nome vecchio → **21/23**, e il rosso **nomina il guasto** (`ReferenceError: activeHelpModule is not defined`). | titolo: `Il popup dei tentativi passa a ui-condivisa` |
| 2026-09-19 | **La rete che mancava: `tests/test_comportamento_audio.js`, il test che GUIDA il Blocco Ascolto e il microfono invece di trovarli.** È la condizione dichiarata nella riga qui sotto, ed è soddisfatta. 13 asserzioni in tre blocchi: [A] il tocco fa partire la voce **nello stesso gestore** e un secondo tocco la ferma; [B] il microfono parte al primo click e **si ferma al secondo**; [C] una registrazione muta **si spegne da sola** e lo studente vede l'avviso. ⚠️ **LA SCOPERTA DEL GIRO, E SPIEGA PERCHÉ 1445 ASSERZIONI NON HANNO VISTO NIENTE: i finti dei test non potevano vedere quei guasti.** Il finto microfono di `test_voicecoach.js` ha `stop() {}` **vuoto** — non ha mai dovuto fermarsi davvero — e il suo finto sintetizzatore **non dichiara `speaking`** (misurati: **17 finti su 41** non ce l'hanno, e `stop() {}` è vuoto in **4** file), mentre `fermaLaVoce()` cancella *solo* se `synth.speaking` è vero: su quei finti `staParlando()` risponde `false` per sempre, quindi `cancel()` non viene chiamato **mai**. *Non era un buco di copertura: era un buco di realtà del mock* — regola 19, e stavolta è costato una regressione su Pages. **Falsificato su tre guasti realistici, ognuno con la previsione scritta prima e ognuno rispettata alla riga:** ① `speakListenBlock` rinviata di un tick (il ritardo visto su Pages) → **9/13**, cadono le quattro righe di [A]; ② il `recognition.stop()` tolto dal secondo click → **12/13**, cade solo quella; ③ il taglio per silenzio disattivato → **11/13**, cadono le due di [C]. ⚠️ **E la prova che conta di più è quella al contrario: con gli STESSI guasti la suite di prima resta verde.** `test_blocco_ascolto.js` 19/19 col Blocco Ascolto rinviato, `test_voicecoach.js` 26/26 e `test_avviso_microfono.js` 31/31 col microfono che non si ferma mai. *Vedere un test morire prova che sa morire; vedere gli altri NON morire prova che il file aggiunge qualcosa invece di ripetere.* **Il «subito» non è cronometrato:** click e lettura stanno dentro **la stessa chiamata sincrona**, quindi la misura non dipende da quanto è veloce la macchina (regola 19) — e in [C] il taglio si **abbassa** invece di aspettarlo, con il tetto lungo alzato apposta perché a fermare la registrazione non sia lui. | titolo: `Il test che guida il Blocco Ascolto e il microfono` |
| 2026-09-19 | **I sette pezzi condivisi sono usciti, hanno rotto Pages, e sono stati riportati indietro.** Lo spostamento in `ui-condivisa` era giusto nel merito — `app/mappa.js` chiedeva `closeAttemptPopup` a un modulo, il verso sbagliato — ma ha prodotto **due regressioni che nessun test vede**: il Blocco Ascolto risponde con ~3 secondi di ritardo, e in Voice Practice la registrazione non si ferma al click, spesso non sente, e supera i 3 secondi. ⚠️ **LE QUATTRO FUNZIONI SPOSTATE SONO IDENTICHE CARATTERE PER CARATTERE** (confrontate con `git show`), il diff di `index.html` è pulito (14 righe aggiunte, 79 tolte, tutte spiegate), il listener globale della regola 16 è intatto e la regex su `activeHelpModule` non ha corrotto niente. **Il codice è lo stesso: è cambiato l'ordine in cui gira.** ~~La causa non è stata trovata.~~ ⚠️ **TROVATA IL 2026-09-19, E NON ERA NEL CODICE: ERA LA CACHE DEL BROWSER.** Vedi la riga «La versione sui quattordici tag». L'app era diventata quindici file con cache indipendenti, e il browser teneva `index.html` nuovo con qualche `app/*.js` vecchio — *un'app che nessuno ha mai scritto*, che spiega parola per parola «funzioni identiche, diff pulito, comportamento cambiato», e perché né il container né la CI l'hanno mai vista (lì i file si leggono tutti insieme dal disco). **Rifatti i tre gruppi a codice fermo, il sintomo non è tornato: né il ritardo del Blocco Ascolto né il microfono** — e il microfono è la prova più pulita, perché il suo gruppo non era ancora stato rifatto quando è tornato a funzionare. **Quindi questo revert ha buttato un lavoro che era già giusto**, e va scritto qui e non altrove: è la riga che qualcuno rileggerebbe per decidere di non rifarlo. ⚠️ **E LA SUITE ERA VERDE**, tre volte di fila, CI compresa. È il limite che il test dello strato dichiarava da sé: *prova che le funzioni sono raggiungibili, non che si comportino come prima.* Nessuna delle 21 righe guida un Blocco Ascolto o un microfono. **È la prima volta che un'estrazione rompe qualcosa che nessun test guarda**, e le due regressioni le ha trovate chi guida il progetto su Pages. Riportati indietro con `git revert` (la storia resta leggibile). **Tenuto `tests/tools/buchi.js`**: è utile e non c'entra col guasto (regola 22). **Tenute le regole 14 e 14-bis**, che erano in commit separati. **La condizione per rifare il passo:** un test che GUIDA il Blocco Ascolto e il microfono — non che li trovi. Finché non c'è, i sette pezzi restano dove sono e i moduli escono con la dipendenza sbagliata dichiarata. | titolo: `I sette pezzi tornano indietro: la suite non guardava il comportamento` |
| 2026-09-18 | **Tre funzioni erano nella regione di Personalizza per POSIZIONE, non per mestiere — e l'estrazione del primo modulo le ha portate via alla mappa.** `migrateCustomizeSeenToModuleProgress` (la chiama solo `openEpisodeMap`), `ensureEpisodeSlotFields` (la chiama `openModuleFromMap` **prima di aprire qualunque modulo**, e la sua cache non è mai uscita), e prima ancora `MAP_PSEUDO_MODULE`. **Dodicesimo e tredicesimo confine corretti dal criterio**, tutti e due contro una mia assegnazione. ⚠️ **E un commento duecento righe più su lo diceva già**: *«non appartengono — `migrateCustomizeSeenToModuleProgress` e `hasStartedEpisodeModules`, che stanno coi progressi e con la mappa»*. Non l'ho letto. *Un avviso scritto accanto non impedisce di non leggerlo* — terza volta in due giorni. **Il criterio che li distingue non è «dove sta», è «chi lo chiama»**: `hasStartedEpisodeModules` sta nella stessa regione e **resta** nel modulo, perché il suo unico chiamante è la schermata di avviso di Personalizza. | titolo: `Chi chiama, non dove sta` |
| 2026-09-18 | **Escape non chiudeva più niente, e il test dello strato aveva dichiarato proprio quel buco come «limite».** Estraendo `app/ui-condivisa.js`, `helpOverlayEl` e `howItWorksOverlayEl` sono finiti dentro lo strato, ma il listener di Escape è rimasto in `index.html` — e deve restarci, perché chiude **tre** overlay e il terzo è del Pannello Admin. Ogni Escape dava `helpOverlayEl is not defined`. ⚠️ **Il test nuovo aveva ventuno righe e nessuna lo vedeva, perché in testa c'era scritto: «LIMITE DICHIARATO: non verifica che i due overlay si chiudano con Escape».** Vero, e irrilevante: *un limite dichiarato dice dove non guardi, non rende innocuo il fatto che non guardi* (regola 42, terza comparsa). **E il confine passava esattamente dove il test aveva smesso di guardare** — che è il posto dove i confini si rompono. L'ha preso `test_new_features.js`, che guida il Pannello Admin da prima di tutto questo. Chiusa con **`chiudiOverlayAperti()`**, e non esponendo i due elementi: *si chiede allo strato di chiudere i propri, non gli si guardano i nodi* — il giorno che nasce un terzo overlay, la funzione lo copre e il chiamante non cambia. Il limite è stato tolto e sostituito da due righe `[E]` che guidano Escape sull'app vera, falsificate togliendo la chiamata. | titolo: `Chi possiede i due overlay sa quali sono aperti` |
| 2026-09-18 | **Un alias su una variabile che CAMBIA — e la riga che doveva impedirlo chiedeva proprio di metterlo.** L'estrazione di `app/dati.js` ha esposto tre variabili riassegnate (`moduleInstructionsCache`, `vcFeedbackDataCache`, `personalizationTablesPromise`). Il ponte degli alias copia il **valore del momento**, cioè `null`, e non lo aggiorna mai più; `uiText()` legge la cache **senza aspettare**; risultato: **ogni testo dell'interfaccia stringa vuota, sette file rossi** — 12 asserzioni su 21 in `test_testi_interfaccia.js`, più le etichette dal JSON in quattro batch, `test_outcome_step_ids` e `test_tabelle_personalizzazione`. ⚠️ **È la stessa forma già incontrata e già chiusa con `moduleEpoch`** (diventato `nuovaEpoca()`/`epocaCorrente()`): un alias congela un valore, una chiamata va a leggerlo adesso. Chiusa allo stesso modo — **`istruzioniInMemoria()`** — e le altre due **non escono affatto**: nessuno le legge da fuori, ed esporle sarebbe lasciare due `null` che sembrano un dato. Esposti 14 → 12. ⚠️ **MA LA CORREZIONE VERA È L'ALTRA, ed è più grande del guasto:** `verificaStruttura` (l'aiutante condiviso dai sei test di strato) chiedeva *«ogni nome esposto ha il suo alias»* — **falso per una classe intera di nomi**, cioè chiedeva esattamente la cosa sbagliata da fare. Nasce **`nomiRiassegnati()`** e l'asserzione che **vieta** l'alias su un nome che lo strato riassegna: +1 asserzione × 6 file, e da oggi il guasto cade sulla riga che lo nomina invece che su sette file lontani. *Nove strati l'hanno passata perché nessuno aveva ancora esposto una variabile che cambia — **un confine sbagliato che non produce nessun rosso è quello che si eredita**, terza volta in tre giorni (le prime due: `renderStars`, `applyEpisodeDialogue`).* Falsificata due volte: ① rimesse le tre esposizioni → la riga nuova nomina **esattamente i tre** nomi del rosso; ② il guasto realistico non è stato simulato, **è stato osservato** — la suite rossa che ha aperto il giro *è* la falsificazione. | titolo: `Un nome che cambia non si aliasa, e adesso la suite lo vieta` |
| 2026-09-18 | **Un'asserzione rossa per una DECISIONE, seguita invece che tolta** (⓪-undecies, terza volta nella serie). La correzione qui sotto ha reso rossa `[A] La pulizia condivisa synth.cancel resta nominata` di `test_pulizie_registrate.js`. **L'invariante non è cambiato** — «la pulizia che ferma la voce resta nominata dentro `stopAllModuleActivity`, perché non ha un modulo che possa registrarla» — **è cambiato il NOME con cui la si nomina**: `synth.cancel` → `fermaLaVoce`. Seguita, con il motivo accanto, e corretta anche la riga «il caso più diverso» in testa al file, che nominava il nome vecchio. *Il rosso è arrivato dalla suite e non dalla previsione: avevo previsto i sei punti di chiamata nell'app e non che un test nominasse la stringa.* | titolo: `La pulizia che ferma la voce ha un nome nuovo` |
| 2026-09-18 | **La chiave degli override torna a essere un punto solo — seconda metà del caso aperto quando `app/avvio.js` è uscito.** Il 2026-09-17 l'estrazione dello strato 0 aveva messo in due file due punti che sapevano la stessa chiave: il **letterale** `'baseinglese:configOverrides'` in `avvio.js` e la costante `CONFIG_OVERRIDES_KEY` in `index.html`. La prima metà (la chiave del tema) si è chiusa con `app/identita.js`; questa aspettava **lo strato che la riunisse**, ed è `dati` — perché `loadPersonalizationTables` la legge per lasciare che il Pannello Admin sovrascriva le tabelle dei nomi. **La chiave vive ora in `app/avvio.js` ed è esposta su `BI`**: sta lì perché lì viene usata per prima — gli override si applicano prima di tutto il resto — e *chi possiede una cosa è chi ha il problema che quella cosa risolve*, non chi la usa di più. Protetta da `[Z]` di `test_config_estratto.js`, che conta il letterale **nel codice di tutta l'app** e ne vuole uno solo; falsificata rimettendolo in `index.html` → cade solo quella riga, **e l'app funziona identica** perché le due stringhe sono uguali. | titolo: `La chiave degli override torna una` |
| 2026-09-18 | **I quattro punti che nominavano Personalizza da fuori diventano uno, dichiarativo.** `migrateCustomizeSeenToModuleProgress` (3 righe) e `hasStartedEpisodeModules` (1) scrivevano `'personalizzazione'` a mano: due funzioni dei progressi e della mappa, che al passo 23 finiranno in file diversi da quello di Personalizza. Nasce **`ID_PERSONALIZZA`**, e la premessa del 23 diventa vera nella sua forma corretta — **non «zero riferimenti», ma «uno solo, e dichiarativo»**: il catalogo deve nominarlo (un passo che nessuno può nominare non si può mettere in sequenza) e il modulo deve dichiararsi. ⚠️ **E la strada scartata, scritta perché è quella che verrebbe in mente per seconda:** usare `type: 'inizio'`, che oggi ce l'ha solo lei. Funzionerebbe, e sarebbe sbagliato — quel campo dice **che etichetta mostrare allo studente**, e fargli dire anche **chi fa da cancello** gli darebbe due mestieri: il giorno che nasce un secondo passo di categoria Inizio i due significati divergono, e uno diventa falso in silenzio (famiglia ⓪-decies). Protetto dal blocco `[E]` **provvisorio** di `test_moduli_registrati.js`, falsificato su due guasti (il letterale rimesso → cade solo quella riga; la costante rinominata → cade solo l'altra, **e l'app continua a funzionare**, perché quell'asserzione fissa il NOME e non la forma — costo dichiarato: il nome È la dichiarazione). | titolo: `Un solo punto dichiarativo per il passo-cancello` |
| 2026-09-18 | **`synth` era una risorsa condivisa da quattro strati futuri, e adesso ne ha uno.** Misurato prima di scrivere: **sette funzioni** lo nominavano — il nucleo audio, la pulizia generale (`stopAllModuleActivity`), Dialogo (`dgTogglePause`, `openDialogo`), Flash Card (`fcFlip`) — più il listener globale del Blocco Ascolto e il pulsante della vista morta. ⚠️ **Non era una duplicazione: era una risorsa condivisa, e una risorsa condivisa non si divide per QUANDO una cosa gira** — cioè l'unico criterio che lo spacchettamento ha. Su di lei il criterio non aveva una risposta. Nascono cinque domande — `vocePossibile`, `staParlando`, `fermaLaVoce`, `pausaLaVoce`, `riprendiLaVoce` — e `synth` resta nominato **solo dentro il nucleo audio**. ⚠️ **Nessun comportamento cambia, ed è il punto:** ognuna è la riga che stava scritta sei volte, con lo stesso `synth &&` davanti. Quel controllo c'era in tutte e sei — il sintetizzatore può non esistere — e bastava che **una** se lo dimenticasse. **10 righe convertite in 6 punti di 5 famiglie diverse.** | titolo: `La voce ha cinque domande, e synth un padrone solo` |
| 2026-09-17 | **La chiave del tema torna a essere un punto solo** — era il caso registrato quando `app/avvio.js` è uscito: lui la scriveva come letterale `'baseinglese:theme'`, `index.html` la conosceva come `THEME_KEY`, e l'estrazione dello strato 0 aveva messo i due punti in **due file**. La condizione diceva «si corregge nello strato che li riunisce»: con `app/identita.js` la chiave vive in un posto, `avvio.js` legge `BI.THEME_KEY`, e il file sta **prima** di lui apposta. Protetto da `[C]` di `tests/test_identita_estratta.js`, che conta il letterale **nel codice di tutta l'app** e ne vuole uno solo. | titolo: `Lo strato identita esce, e la chiave del tema torna una` |
| 2026-09-17 | **La storia dei diff di `BASELINE-AVVIO.txt` si cancellava da sola.** Scritta a mano dentro il file, **è sparita alla prima rigenerazione**: `scrivi-baseline-avvio.js` riscrive l'intestazione per intero. *Una difesa che sparisce quando lo strumento gira è una difesa che non c'è — e non somigliava a un guasto, somigliava a un file aggiornato (famiglia della regola 37).* La storia è ora un dato dello script, quindi ogni rigenerazione la riporta. **Trovata per caso**, rigenerando il baseline per lo strato dopo: se l'estrazione successiva fosse arrivata fra un mese, il file sarebbe stato senza storia e nessuno avrebbe saputo che ce n'era stata una. | titolo: `Lo strato identita esce, e la chiave del tema torna una` |
| 2026-09-17 | **La regola 37 violata da chi l'aveva appena letta, con il `pkill -f` per nome — ed è la SECONDA volta che questo difetto passa, dopo il 2026-09-15 che l'ha fatta nascere.** Ho scritto `pkill -f "node tests/serve.js"` all'inizio della riga che poi rilanciava la suite: **la riga di comando della shell contiene quella stringa**, quindi `pkill` ha trovato sé stesso e l'ha terminata (uscita 144). Il `rm` e il `nohup` che venivano dopo **non sono mai partiti**. ⚠️ **E il risultato è esattamente quello che la regola descrive: `tests/suite.log` era quello del giro PRECEDENTE, cioè un risultato vecchio che somiglia a un risultato nuovo** — `attendi.sh` ci ha letto «SOME FILES FAILED» e io stavo per diagnosticare un rosso già corretto. **Quello che l'ha smascherato non è stato un controllo, sono stati i tempi:** log delle 21:28:30, correzione del file di test alle 21:29:19. *Il log era più vecchio della correzione di cinquanta secondi.* **Non ho toccato la regola**: non mancava la regola, mancava che la applicassi — è la stessa forma della 19 il 2026-09-10. La terza corsa, lanciata senza nessun `pkill`, è verde. | titolo: `Lo strato progressi esce: app/progressi.js` |
| 2026-09-17 | **Nove lettori e dieci scrittori del magazzino erano la stessa funzione scritta nove volte — chiusa PRIMA di estrarre lo strato, non registrata accanto.** Due coppie erano identiche **carattere per carattere** a meno della chiave: `loadMastery`/`loadModuleOutcomes` e `loadAudioUsage`/`loadNextLineSkips`. Nascono `leggiMagazzino(chiave, vuoto, valida)`, `scriviMagazzino(chiave, valore)` e `cancellaMagazzino(chiave)`: **le chiamate dirette a `localStorage` per le dodici chiavi di progresso passano da 21 a 3**. ⚠️ **`vuoto` è una funzione e non un valore**: passando `{}` ogni chiamante riceverebbe lo stesso oggetto e il primo che lo modifica lo modifica per tutti — un difetto che non alza eccezioni e si vede giorni dopo come un progresso comparso da solo. ⚠️ **Una differenza di comportamento dichiarata**: un JSON «falso» salvato (`null`, `0`, `""`) prima veniva restituito da due lettori, ora torna il vuoto — **nessuno dei dieci scrittori può produrlo**, quindi non è osservabile, ed è nella direzione sicura. **Restano fuori di proposito** `isCustomizeSeen`, `isIntroDismissed`, `setIntroDismissed`: tengono la stringa `'1'`/`'0'`, non JSON, e farli passare vorrebbe dire cambiare il formato salvato o dare al punto unico una seconda modalità (famiglia ⓪-decies). ⚠️ **E l'invariante della mastery è stato SEGUITO** (⓪-undecies): «un solo punto scrive» ora vive dentro `scriviMagazzino`, quindi `test_mastery_al_gesto.js` ha una riga in più che vieta a chiunque altro di passargli `masteryStorageKey` — **falsificata: cade solo lei, le due sul conto di `saveMastery` restano verdi**, cioè non potevano vedere il guasto. | titolo: `Il magazzino ha tre punti invece di ventuno` |
| 2026-09-17 | **La categoria della chiave mastery era sbagliata, il fatto no — corretta dove era scritta.** Avevo registrato «il punto unico aggirato». Non lo è: scavalcare un punto unico vuol dire avere la funzione a portata di mano e non chiamarla, mentre `masteryStorageKey` **non è raggiungibile** da un `page.evaluate` (vive dentro l'IIFE). La categoria giusta è **due lettori indipendenti dello stesso spazio**, la stessa della chiave del tema. ⚠️ **E non è una differenza di parole: un punto unico aggirato si corregge dicendo a qualcuno di chiamare la funzione; due lettori indipendenti si correggono solo dando loro una fonte comune, cioè esponendola** — la categoria sbagliata indicava il lavoro che non funziona. ⚠️ **E rimisurando è emerso che il caso della mastery non era un caso: era il campione più piccolo di una famiglia da 197** (setaccio sulle dodici chiavi di progresso: **nell'app ZERO accessi fuori dai costruttori**, nei test **197 occorrenze dei letterali** — `introDismissedKey` 75, `moduleProgressKey` 65, mastery 13). *L'avevo misurato perché me l'avevano nominato, e misurare quello che viene nominato dà sempre un caso isolato.* | titolo: `Il setaccio sulle dodici chiavi, e la categoria della mastery` |
| 2026-09-17 | **La lettura della CI poteva essere onesta e sbagliata lo stesso, e la difesa non era «ricordarsi di usare lo strumento».** Si può leggere il campo giusto di **un'altra corsa**: un elenco risponde il vero su ognuna, e quella che interessa è una sola. Adesso **ogni esito di `attendi-ci.sh` nomina il commit per intero**, e accanto porta la riga *«se non è quello che hai appena spinto, hai letto un'altra corsa»* — così chi legge il resoconto può confrontare **senza fidarsi di chi gliel'ha passato**. ⚠️ **E una cosa misurata prima di toccare lo script: l'uscita 4 («non riesco a vedere») era l'UNICA dei cinque esiti che il commit non lo nominava** — diversa per **assenza**, quindi è lei il caso su cui il giro di `[H]` vale davvero (regola 42). Dieci asserzioni nuove in `tests/test_attendi_ci.js` (21 → 31), viste cadere su due guasti separati: togliendo la riga all'uscita 4 cadono **esattamente 2** righe, togliendo la frase di confronto a tutte cadono **esattamente 5** e le cinque sul commit restano verdi — *nessuna delle due asserzioni copre l'altra*. | titolo: `Ogni esito della CI nomina il commit, e dice cosa farsene` |
| 2026-09-17 | **Due commenti di `index.html` resi falsi dalla separazione di `showView`/`leaveModule`, e NON corretti nel commit che li ha resi tali.** ① sopra `moduleEpoch`: «Bumped on every `showView()` call (i.e. every navigation)» — il `++` sta in `leaveModule`, cioè **dodici** navigazioni su quattordici; le due `showView('onboarding')` non lo alzano, ed è giusto (lì nessun modulo è mai stato aperto). ② nel banner della schermata d'errore: «`showView()` chiama già `stopAllModuleActivity()`» — non più: `showLoadError` ci arriva passando da `leaveModule`. **Nessuno dei due descriveva male il proprio codice: sono diventati falsi perché è cambiato il mondo intorno** (famiglia ⓪-quinquies), ed è per questo che una rilettura del diff non li prendeva — il diff di quel commit non li toccava. Trovati riconoscendo lo strato `vista`, cioè leggendo quel codice per un altro motivo. ⚠️ **E il secondo è ANCHE l'unica riga di prosa che `righeDiCodiceDi()` lascia passare contenente un identificatore misurabile** (`stopAllModuleActivity`): la stessa riga era falsa *e* visibile allo strumento che deve vedere solo codice. | titolo: `I due commenti falsi, la riconta di vista, e il limite del filtro` |
| 2026-09-15 | **Il Pannello Admin descriveva la chiave della mastery come `esercizio:voce:direzione`, e per Flash Card il primo campo non è l'esercizio.** È `flashcard-A` / `flashcard-B` — **porta anche il grado**, perché dal passo 4 Flash Card compare due volte nella sequenza su gradi diversi e le due apparizioni non devono mescolare i colori. Gli altri due lettori rispettano la forma dichiarata (`match:<voce>:<direzione>`, `speedmatch:<voce>:<direzione>`), quindi la descrizione era vera per due su tre. Il testo diceva già l'eccezione di Voice Practice/Voice Check; **mancava questa**. Trovato riderivando il «testo falso in `renderMasteryPanel`» che il passo 17 dichiarava senza dire quale frase fosse — e le tre affermazioni che il passo poteva intendere sono risultate tutte **vere** (i due prefissi, il numero di scritture che non c'è, gli id `a-hello` e `b-and-you`). | titolo: `Il 17 non si fa: due setacci, due prove negative, e la chiave del pannello` |
| 2026-09-10 | **Il modulo si apriva prima di avere quello che gli serve.** `openVoiceCoach` disegnava la schermata e **abilitava il microfono** mentre la battuta non era ancora arrivata: premendolo in quella finestra, `tokenize(vcTargetText())` moriva con `Cannot read properties of null`. La causa non era «si apre presto»: era un **precaricamento travestito da dipendenza** — il `Promise.all` aspettava `loadFeedbackMessages()` e `results[1]` **non veniva mai letto**. Tolta quella riga la finestra passa da **17 ms di mediana a 1 ms**, e con la rete lenta simulata (2 s) il `TypeError` da 3 volte su 3 a nessuna. In più `openModuleFromMap` ora aspetta anche `loadEpisodeData`, che è il punto unico da cui passano tutti i moduli: **beneficio dichiarato teorico nel codice**, perché chi lo trova non lo tolga credendolo inutile. ⚠️ La finestra valeva per **tutti e otto** i moduli — misurato uno per uno — non per Voice Coach: gli altri sette accettavano un gesto che non faceva niente. Protetto da `tests/test_modulo_pronto.js`. | `c456b9d` |
| 2026-09-10 | **La guardia sa che Personalizza non legge un file episodio.** La riga aggiunta a `openModuleFromMap` (qui sopra) chiedeva `loadEpisodeData` a **tutti** i moduli, ma Personalizza è **l'unico dei sedici senza `dataFile`**: `fetch(undefined)` falliva, il `.catch` apriva `showLoadError`, e la schermata d'errore compariva su un modulo che funziona. La condizione si legge ora dal descrittore (`module.dataFile`), non da un elenco a parte da tenere allineato a mano.<br><br>⚠️ **E va scritta la parte scomoda: era una regressione introdotta e corretta nello stesso giro, e il buco stava nel test appena scritto.** `test_modulo_pronto.js` guidava **solo Voice Coach** — cioè il caso più simile a quello che stavo scrivendo, quello che avevo già montato e aperto. Cinque file rossi alla prima corsa (`test_batch2`, `test_batch3`, `test_batch3b`, `test_batch16`, `test_outcome_step_ids`). Il blocco `[C]` ora apre Personalizza dalla mappa. **Da qui la regola 42 di `CLAUDE.md`**: un test che guida un caso solo non protegge una riga che vale per tutti. | `a6e3f97` |
| 2026-09-05 | La regola di esito (`CONFIG.moduleOutcomeRules`) veniva cercata con l'**id del passo** invece che con l'id del **modulo**: dalla seconda apparizione in poi (`quickMatchEngIta-2`, `flashcardAEngIta-2`, `voicePractice-2`…) la chiave non esisteva, `saveModuleOutcome` non veniva chiamato e sei passi su ventidue restavano senza colore in mappa. Sette letture passate a `.moduleId` — un campo che l'app costruiva già su ogni passo e non leggeva nessuno. Era il § 4.1 di `docs/validazione.md`. | titolo: `La regola di esito arriva a ogni apparizione, non solo alla prima` |
| 2026-09-05 | `CONFIG.attemptRule` tolta. Le sue due righe (`voicePractice: 'lastAttempt'`, `voiceCoach: 'firstAttempt'`) duplicavano una distinzione già nel descrittore come `module.voiceVariant`, ed erano anch'esse indicizzate per id del passo: il passo 16 cadeva su `undefined` e usava il primo tentativo invece dell'ultimo. La regola vive ora in `vcEvaluate`, che rama su `vcVariant`. | titolo: `La regola di esito arriva a ogni apparizione, non solo alla prima` |

## Episodi di forma diversa (i tre difetti silenziosi)

*Tutti e tre chiusi insieme il 2026-09-08, perché sono la stessa famiglia: non
crollano, sembrano funzionare, e mordono al primo episodio che non ha la forma
del primo. Sono diventati urgenti con la decisione degli **episodi corti** —
solo gradi C e D.*

| Data | Cosa | Commit |
|---|---|---|
| 2026-09-08 | `openModuleByKind` aveva una catena di `if/else if` **senza ramo finale**: un passo con un `kind` sconosciuto era una riga cliccabile che non faceva nulla, restava "attuale" per sempre e bloccava tutti i successivi senza un errore in console. Ora il ramo finale mostra la schermata d'errore, che è già la strada per «qualcosa non va» (regola 35). | titolo: `Tre difetti silenziosi che aspettavano il primo episodio corto` |
| 2026-09-08 | `episodeGrade()` torna `[]` per un grado assente, e i costruttori di coda leggevano quel `[]` come «coda già finita»: il modulo si dichiarava **completato senza far fare un solo esercizio**, e registrava l'esito. Nasce `episodeGradeRequired()`, il lettore che **pretende**: sta nei sette punti dove il contenuto è indispensabile (cinque code e due schermate), alza, e il fallimento cade nella `.catch()` che ogni modulo ha già. `episodeGrade()` resta il lettore neutro per i gestori di click, dove «nessuna voce» vuol dire solo «niente da fare». | titolo: `Tre difetti silenziosi che aspettavano il primo episodio corto` |
| 2026-09-08 | `migrateCustomizeSeenToModuleProgress()` scriveva `'personalizzazione'` nei progressi di **qualunque** episodio, anche di uno che quel passo non lo dichiara. Ora controlla che l'episodio abbia davvero quel modulo. | titolo: `Tre difetti silenziosi che aspettavano il primo episodio corto` |

## Suite di regressione

| Data | Cosa | Commit |
|---|---|---|
| 2026-09-18 | **L'ordine dei tag ha rotto qualcosa per la prima volta, e un `.catch` ha nascosto perché.** `app/personalizza.js` era in prima fila (`<head>`) e prende i suoi alias — `slotOptions`, `uiText`, `fillTemplate` — **a tempo di parsing**; `ui-condivisa.js` carica nella seconda fila, quindi `BI.slotOptions` non esisteva ancora. Personalizza moriva con `slotOptions is not a function`. ⚠️ **E l'errore non si vedeva:** il `.catch` di `openCustomize` avvolge anche il `.then`, quindi qualunque eccezione dentro `renderSlotGrid` diventa «schermata d'errore» **senza un messaggio in console** — famiglia `ERRORI-INGOIATI`. Per trovarlo ho dovuto far parlare il `catch` a mano. *È la prima volta che l'ordine dei tag rompe qualcosa davvero, ed è successo il giorno dopo che la regola 45 lo aveva reso dichiarabile.* Risolto spostando il tag nella seconda fila, dopo `ui-condivisa`. | titolo: `Il tag di personalizza va dopo ui-condivisa` |
| 2026-09-18 | **La misura delle dipendenze non poteva dire «parsing»: la risposta era impossibile, non sbagliata.** `tests/tools/dipendenze.js` contava le graffe **dal primo carattere del file**, ma ogni file di `app/` è avvolto in `(function (BI) { … })(window.BI);` — quindi la riga dopo la prima è già a profondità 1 e **tutto** usciva «a tempo di chiamata». ⚠️ **L'asserzione «nessuna dipendenza a tempo di parsing» era vera per costruzione**, cioè il difetto della regola 37 nella sua forma peggiore: *non somigliava a un errore, somigliava a un risultato* — e per un giorno intero ha dato il verde a una domanda che non veniva posta. Trovata perché `app/ui-condivisa.js` ha la **prima dipendenza di parsing vera** del progetto (quattro alias in cima all'IIFE) e la misura continuava a chiamarla «chiamata». Corretta: la profondità parte da −1, cioè si conta **dentro** l'IIFE. *Quinta correzione di questa misura in due giorni, e la sola che non lasciava né un buco né un'accusa: rendeva una risposta irraggiungibile.* | titolo: `La profondita' si conta dentro l'IIFE` |
| 2026-09-18 | **`git checkout --` non ripristina un file NON TRACCIATO, e la falsificazione era ancora in piedi.** Dopo il secondo guasto su `app/orchestrazione.js` (una vista tolta dall'elenco) il ripristino sembrava fatto, ma il file era nuovo e mai committato: `git checkout` non ha toccato niente e la riga è rimasta via. ⚠️ **Terza comparsa della stessa famiglia in due giorni** — le prime due su file *tracciati*, dove il comando ha invece ripristinato **troppo**, portandosi via anche la modifica buona (`index.html`, poi `app/audio.js`). *Lo stesso comando sbaglia in due versi opposti a seconda che il file sia tracciato o no, e in nessuno dei due casi lo dice.* **Presa dalla previsione, non dall'occhio:** avevo scritto che il ripristino doveva dare **16/16**, l'osservato era **14/16**, e lo scarto ha trovato il file mai ripristinato. *Senza il numero scritto prima, avrei committato una falsificazione al posto del codice.* La difesa non è ricordarsi la regola: è **scrivere il conto atteso prima di ripristinare**, che è quello che ha funzionato tutte e tre le volte. | titolo: `Un file nuovo non si ripristina con git checkout` |
| 2026-09-18 | **L'ordine dei tag `<script>` era un vincolo VERO e non scritto da nessuna parte — adesso ogni file lo dichiara e la suite lo verifica.** `app/avvio.js` legge `BI.THEME_KEY`, che appartiene a `app/identita.js`, e funziona **solo** perché identita è caricato prima: nessun commento lo diceva, nessun test lo guardava. Nascono **`tests/tools/dipendenze.js`** (misura il grafo dal codice, distinguendo il momento: **parsing** obbliga l'ordine, **chiamata** no) e **`tests/test_dipendenze_dichiarate.js`** (24 asserzioni), più la riga `// DIPENDE DA:` in testa a tutti e nove i file di `app/`. ⚠️ **È lo stesso passaggio già fatto tre volte — da «ricordarsene» a «lo dice il file»:** la guardia di `hidden` (regola 12), i percorsi di `test-env.js` (regola 24), il divieto di alias sui nomi riassegnati. *Una frase che descrive un comportamento cede; un confronto no.* **Il grafo vero, misurato:** `avvio.js` → `identita.js`, `dati.js` → `avvio.js`, `dati.js` → **`index.html`** (`applyEpisodeDialogue`). Tre in tutto, tutte a tempo di chiamata, e **l'ultima è all'insù** — il verso che la serie esiste per eliminare, quindi quel conto deve calare e mai salire. ⚠️ **E LA MISURA SI È SBAGLIATA QUATTRO VOLTE prima di stare in piedi, tutte e quattro scritte dentro lo strumento:** ① cercando il nome **nudo** il grafo usciva **vuoto**, perché fra file ci si parla con `BI.nome` e il punto davanti escludeva proprio quelli — *zero dipendenze sembra un bel risultato*; ② `avvio.js` espone con `window.BI.x = x` e la riga che raccoglieva i nomi non lo vedeva; ③ un `BI.nome` che nessun file di `app/` possiede non è zero, è una dipendenza **verso `index.html`**; ④ **possedere non è esporre** — `app/spazio.js` scrive `BI.pulizie = []`, quindi risultava **dipendere da `index.html` per sei nomi suoi**: la misura accusava il file che li possiede. *Le prime tre lasciavano un buco, la quarta un'accusa.* Falsificato su tre guasti distinti: dichiarazione vecchia (1 rossa, che stampa il vecchio **e** il misurato), tag riordinati (1 rossa), dipendenza nuova all'insù (3 rosse insieme). | titolo: `Ogni file dichiara da chi dipende, e la suite lo confronta col codice` |
| 2026-09-18 | **Un'asserzione rossa per una DECISIONE, seguita invece che tolta** (⓪-undecies, **quinta** volta). `test_tabelle_personalizzazione.js` cercava `var PERSONALIZATION_TABLES_FILE = '` **in `index.html`**, e il giorno prima quella costante era andata in `app/dati.js` con gli altri tre percorsi. L'invariante non è cambiato — *il percorso sta in una costante, non dentro una riga di `fetch`* — è cambiato **dove lo si va a leggere**. ⚠️ **E il divieto del `fetch` nudo è rimasto su ENTRAMBI i file**: toglierlo da `index.html` perché «tanto lì non ci sono più `fetch`» renderebbe la riga **vera per costruzione** il giorno in cui uno tornasse. *Dei sette file rossi del giro, sei venivano dall'alias e **uno solo** era questo: due difetti diversi nello stesso commit, e il secondo si vedeva solo dopo aver tolto il primo.* | titolo: `L'asserzione segue la costante che si e' spostata` |
| 2026-09-16 | **Il blocco `[C]` di `test_listener_una_volta.js` ciclava tre famiglie su sette, e le quattro escluse erano coperte da nessun altro.** Per una famiglia a `kind` **singolo** — Repeat Aloud, Personalizza — `[C]` è l'**unico** blocco che vede una guardia mancante: `[A]` apre ogni famiglia una volta sola (quindi non duplica) ed `[E]` guarda solo chi ha più `kind`. Misurato togliendo la guardia durante il giro ④: Repeat Aloud **restava verde**. Ora `[C]` cicla tutte e sette le famiglie con un ritorno alla mappa, ed è derivato da `FAMIGLIE` invece che da un elenco scritto a mano. ⚠️ *Un campione va bene quando i casi si somigliano: qui il caso che il campione escludeva era proprio quello che nessun altro blocco copriva.* | titolo: `21-quater ④ — Repeat Aloud, e la schermata che nessuno apre` |
| 2026-09-16 | **`toccaFinoA` contava le MOSSE invece dei TOCCHI, e il budget se ne andava in diramazioni che non toccano niente.** Il ciclo ha cinque strade che consumano una mossa senza toccare un'opzione (popup, ripasso, riquadro aperto, ultima domanda). Misurato sul rosso vero: `mosse 41/41 | spese in {popup:1, ripasso:2, riquadro:19, ultimaDomanda:2, tocchi:17}` — **ventiquattro mosse su quarantuno non hanno toccato niente**, e ne restavano diciassette. Sbagliare 17 volte di fila fra quattro opzioni è **(3/4)^17 ≈ 1 su 133**: raro, non impossibile, e su una suite quotidiana si presenta. Contando i tocchi il budget torna quello che il conto voleva dire — (3/4)^41 ≈ 7 su un milione. Il tetto sulle mosse resta, largo, solo contro un ciclo incastrato. ⚠️ **Non è stato chiamato flake:** il motivo era già stampato da `arrenditi()` nel `.result.txt`. *Quello che mancava non era la diagnosi: era guardarla — ho passato sei giri a cercare di riprodurlo prima di leggere la riga che il test aveva già scritto.* | titolo: `toccaFinoA conta i tocchi, non le mosse` |
| 2026-09-16 | **Due buchi nel test del 21-quater, trovati falsificando PRIMA che una conversione cominciasse — ed è tutto il senso del test-first.** ⓵ Le attese si derivavano dal **sorgente**, cioè dallo stesso file da controllare: togliendo la riga di un listener spariva anche l'attesa, e il test restava verde. *Si misurava contro sé stesso — la forma dell'asserzione vera per costruzione (regola 44), spostata dall'asserzione all'elenco su cui gira.* Ora il confronto è con `tests/BASELINE-LISTENER.txt`. ⓶ Il baseline diceva solo «c'è / non c'è», e i sette `*-complete-btn` hanno **due** listener (il suono d'uscita di `renderSummaryScreen` e il completamento): spegnendo il completamento il conto scende da 2 a 1 e il pulsante **suona senza completare** — provato, e passava verde. Ora il baseline porta il **numero**, misurato **guidando l'app** perché il listener del suono non compare in nessuna riga del sorgente. | titolo: `Passo 21-quater — il test, prima della prima conversione` |
| 2026-09-16 | **Un modulo con `dialogoProfile` e un `kind` sconosciuto si apriva lo stesso.** Quattro rami su otto di `openModuleByKind` dispacciavano su una PROPRIETÀ del descrittore invece che sul `kind`, quindi il controllo che protegge gli episodi corti — «un `kind` che non corrisponde a niente non deve diventare una riga cliccabile e muta» — **valeva per metà dei moduli**. Ora il `kind` è la chiave unica e il caso sconosciuto va alla schermata d'errore. ⚠️ **È un cambio di comportamento voluto, non una conseguenza:** se un giorno un episodio smettesse di aprirsi, è la prima riga da leggere. | titolo: `Passo 21-ter — i moduli si risolvono dal registro` |
| 2026-09-16 | **Una pulizia che esplodeva non rompeva una pulizia: rompeva l'app.** L'eccezione risaliva da `stopAllModuleActivity` a `showView`, che è il punto unico di ogni navigazione — misurato: con una pulizia rotta non si riesce nemmeno a **entrare** in un modulo, non solo a uscirne. Ora ogni pulizia gira in un `try/catch`, il fallimento va in console col nome della pulizia, e la navigazione arriva sempre in fondo. ⚠️ **Il catch da solo sarebbe stato uno scambio in perdita** — un guasto rumoroso cambiato con uno silenzioso: la metà che lo rende accettabile è l'asserzione di `test_pulizie_registrate.js` che diventa rossa se il catch smette di riportare, e quella asserzione **mancava al primo giro** (il test restava verde col catch reso muto). | titolo: `Passo 21-bis — le pulizie si registrano` |
| 2026-09-16 | **Una verifica per sottrazione che non distingueva il codice dalla prosa.** Il primo controllo su `stopAllModuleActivity` segnalava `dgClearAllTimers: 1` — ed era il nome citato dentro un **commento** che spiega la misura dell'ordine, non una chiamata. Una misura che non misura (regola 37) messa a guardia del passo che *toglie nomi*. I commenti si tolgono prima di cercare, nel controllo e nel test. | titolo: `Passo 21-bis — le pulizie si registrano` |
| 2026-09-16 | **Un'invariante della catena era scritta al contrario, e a scriverla al contrario ero stato io quattro giorni prima.** Diceva *«il passo 21 non è una fermata sicura: o l'oggetto esiste e tutto ci passa attraverso, o no»* — vero per il passo 21 di prima, falso da quando (2026-09-15, la fase 4 rifatta) il «tutto ci passa attraverso» è stato staccato in 21-bis, 21-ter e 21-quater. **Chi ha rifatto il piano non è tornato a guardare le invarianti che quel piano dava per ferme.** Corretta, e con la regola che ne esce: *quando si rifà un piano, si rileggono le invarianti che quel piano cita — sono la parte che nessuno guarda, proprio perché si chiamano invarianti*. ⚠️ Non l'ha trovata una rilettura: l'ha trovata una **domanda esplicita**, ed è il motivo per cui la regola dice *rileggere*, non *ricordarsi*. | titolo: `Passo 21 — lo spazio dei nomi` |
| 2026-09-15 | **Il commento di `APP_CONFIG_DEFAULTS` nominava un consumatore che fa un'altra cosa.** Diceva di servire al pulsante «Ripristina valori di partenza»; quel pulsante cancella la chiave degli override e ricarica, e `DEFAULTS` non lo tocca — una sola occorrenza in tutto il repository, la sua assegnazione. **Corretto il commento, non la riga:** toglierla è una rimozione (regola 1), registrata in `docs/decisioni.md` con la sua condizione. ⚠️ Trovata **leggendo la funzione che il commento indicava**, non la riga — il metodo della famiglia ⓪-sexies applicato il giorno dopo averlo scritto. | titolo: `Passo 20 — APP_CONFIG esce in app/config.js` |
| 2026-09-15 | **`tests/module-order.js` leggeva il magazzino dal TESTO di `index.html`**, con una regex sul sorgente — non `CONFIG.people`, non un fetch: il file come stringa. Uscite le tabelle, `readTable` tornava `null`, i segnaposto restavano non sostituiti e il vocabolario atteso diventava `"I am {{papa}}."` contro `"I am Marco."` a schermo. **Due file rossi** (`test_batch15`, e `test_interruttore_episodio` con cinque asserzioni in meno). ⚠️ **Il lettore era stato guardato e classificato male:** la ricerca aveva trovato la riga `slot.table.indexOf('people.')` e concluso «legge lo slot, non le tabelle» — vero per quella riga, falso per la funzione intorno. Adesso legge il JSON, che è anche la forma giusta: un JSON si parsa, non si cerca con una regex. | titolo: `Passo 19 ① — le tre rosse della suite` |
| 2026-09-15 | **Il Pannello Admin richiudeva sotto le dita il gruppo che stavi guardando.** I due gruppi del magazzino arrivano dopo l'apertura, e la prima versione li faceva comparire con un `renderConfigPanel()` completo: `innerHTML = ''` azzera il `<details>` che l'utente ha appena aperto. Trovato da `test_interruttore_episodio` — un file che col magazzino non c'entra niente — che apre il gruppo e poi cerca il menu dentro. Ora i due gruppi si **aggiungono** (`aggiungiGruppiMagazzino`, con `renderConfigGroup` estratta e riusata da entrambe le strade). | titolo: `Passo 19 ① — le tre rosse della suite` |
| 2026-09-15 | **Un'asserzione mia, vera per costruzione, trovata falsificandola** (regola 44). La riga che difende il punto qui sopra leggeva `document.querySelector('.config-group')`: il primo `.config-group` del documento sta **fuori** dal corpo del pannello — misurato, **27 nel documento contro 23 dentro** — quindi il ridisegno non lo tocca mai e `open` restava `true` qualunque cosa facesse il codice. ⚠️ *A cadere non è stata l'asserzione: è stata la **falsificazione**, che è passata verde. Senza quel giro la riga sarebbe entrata nella suite sembrando una difesa.* Selettore ora ancorato a `#config-panel-body`. | titolo: `Passo 19 ① — le tre rosse della suite` |
| 2026-09-15 | **`test_batch16.js` leggeva una chiave che non esiste.** Cercava `cfg.places.partenza`: la tabella si chiama `departures`, `partenza` è il nome dello *slot* che la usa. Quindi `torinoOpt` era sempre `undefined` — **calcolato e mai usato** — e `placesTableExists` sempre `false`, **calcolato e mai asserito**. Nessuna asserzione cadeva: è la misura che non misura (regola 37) dentro un test, sopravvissuta perché non faceva rumore. L'esca vera (`people.papa.francesco` con `en` diverso da `it`) ora si legge dal file del magazzino. | titolo: `Passo 19 ① — il magazzino esce da APP_CONFIG` |
| 2026-09-15 | **`test_config_letta.js` avrebbe perso metà copertura restando verde.** Il suo inventario nasce da `Object.keys(window.APP_CONFIG)`: nel momento in cui `people` e `places` ne escono, le loro sette tabelle spariscono dall'elenco **senza un rosso e senza un avviso**, e il test continua a dire «ogni chiave è nominata da qualcuno» su un insieme più piccolo. Ora legge anche il file del magazzino, con un'asserzione che dichiara la copertura invece di darla per scontata. Verificato che il numero non cala: **144 chiavi controllate prima e dopo** (137 da `APP_CONFIG` + 7 dal magazzino). | titolo: `Passo 19 ① — il magazzino esce da APP_CONFIG` |
| 2026-09-15 | **Giro B: le due frasi dell'intro erano ricopiate DIECI volte, e adesso sono due chiavi.** ⚠️ **Non era solo una misura: era un difetto.** Dieci copie della stessa frase sono dieci occasioni di scriverne una diversa — e il giro le chiude di passaggio, senza che fosse il suo scopo. `condivisi.introStart` e `condivisi.introDontShowAgain` (più `readyTitle`/`readyStart` per il ramo «già vista») sostituiscono 9 pulsanti + 10 caselle sparsi fra markup e JS. | titolo: `Giro B del passo 18: l'intro condivisa, e l'attesa che copre tutto` |
| 2026-09-15 | ⚠️ **L'attesa della schermata di intro copriva solo il CORPO, e adesso copre tutto quello che quella schermata mostra.** `renderIntroContent` scriveva «Caricamento...» nel corpo e lasciava casella e pulsante come markup statico: portandoli nel file sarebbero rimasti **due etichette mute accanto a un messaggio che non le riguardava** — peggio che non spostarle. Ora il pulsante resta **disabilitato** finché non c'è tutto e prende l'etichetta insieme al corpo; se il file non arriva resta spento, perché *«Ho capito» non è una cosa che lo studente possa dire se la spiegazione non è arrivata*. | titolo: `Giro B del passo 18: l'intro condivisa, e l'attesa che copre tutto` |
| 2026-09-15 | **Tre testi che il GIRO A aveva mancato, trovati facendo il giro B.** `qm-start-btn`, `dg-start-btn` e `sr-ready-btn` ricevevano *«Ho capito, inizia»* da JS — cioè testi scritti **su richiesta**, l'ambito esatto del giro A. Il censimento non li aveva visti perché il filtro pretendeva una parola-funzione italiana e *«Ho capito, inizia»* non ne ha nessuna: è la stessa falla già dichiarata su `"Invia"` e `"Richiesta urgente"`. Lasciarli sarebbe stato il caso che «mai a metà» vieta davvero — **la stessa etichetta da due sorgenti nello stesso pulsante**. | titolo: `Giro B del passo 18: l'intro condivisa, e l'attesa che copre tutto` |
| 2026-09-15 | ⚠️ **Il primo blocco di test del giro B era VERO PER COSTRUZIONE, e l'ha rivelato solo il guasto iniettato.** Apriva la mappa e guardava subito: misurato, il fetch del JSON parte all'apertura della mappa e **ha già risolto al primo istante in cui Playwright riesce a leggere qualcosa**, quindi il test restava 19/19 anche togliendo il `disabled` dal codice. *Una finestra che si chiude più in fretta di un round-trip non si osserva correndole contro: si **riproduce**.* Ora il blocco rallenta la rete a 400 ms con `page.route` e guarda DENTRO la finestra. Controprova su due guasti: tolto il `disabled` → **17/21**, con la prova esatta (`spento:false, testo:""`); rimessa una copia dell'etichetta nel codice → **20/21**, un rosso solo e sul punto giusto. | titolo: `Giro B del passo 18: l'intro condivisa, e l'attesa che copre tutto` |
| 2026-09-15 | **Il giro A del passo 18: i testi che l'app scrive SU RICHIESTA escono dal codice.** Pannello Help (14 testi), gli `aria-label` del Blocco Ascolto (3), Voice — didascalie del microfono, avviso di silenzio, i tre livelli dell'avviso, trascrizione, etichetta di ritentativo (13) — e Dialogo (4). Il meccanismo è **`uiText(percorso, ultimaIstanza)`**, che legge la cache **senza aspettare**: molti di questi testi li scrivono funzioni sincrone (`setVcState` cambia la didascalia mentre registri) e un fetch li farebbe cambiare un frame dopo il gesto. ⚠️ **L'ambito è «su richiesta» e non «nel JS», ed è una scoperta del giro:** `renderChoiceBox` e `renderSummaryScreen` girano a tempo di **parsing**, quindi hanno lo stesso rischio del markup e sono passati al giro B. L'asse che decide il pericolo non è dove sta il testo, è **quando viene scritto**. | titolo: `Giro A del passo 18: i testi su richiesta escono dal codice` |
| 2026-09-15 | ⚠️ **Una motivazione FALSA scritta accanto a codice giusto, e l'ho scritta io un'ora prima.** Il commento di `uiText()` diceva *«funziona perché `openModuleFromMap` fa già `Promise.all([loadEpisodeData, loadModuleInstructions])`»*. **Non lo faceva**: aspettava `ensureEpisodeSlotFields` e `loadEpisodeData`, non i testi. Risultato misurato: il modulo si apriva con gli `aria-label` del Blocco Ascolto e la didascalia del microfono **vuoti**. Corretto aggiungendo `loadModuleInstructions()` a quel `Promise.all` — se il file non arriva, il `.then` non gira e il modulo **non si apre**: si va alla schermata d'errore, che è la scelta della regola 35 (*un'interfaccia che si svuota non è un caso da gestire, è un caso da rendere impossibile*). ⚠️ **Trovata GUIDANDO l'app, non rileggendola** — e nessuna rilettura l'avrebbe presa, perché la frase descriveva esattamente il codice che serviva, solo che quel codice non c'era. *Una motivazione falsa accanto a codice giusto è la cosa peggiore da lasciare in giro: chi la legge smette di controllare.* | titolo: `Giro A del passo 18: i testi su richiesta escono dal codice` |
| 2026-09-15 | **Un fallback di testo che era già divergito dalla sua fonte.** In `submitCustomizationRequest` c'era `entry.requestBoxDuplicateWarning || 'Questa parola è già tra le opzioni disponibili.'` — cioè la stessa frase del JSON **meno la sua seconda metà** («— prova a selezionarla direttamente dal campo sopra»). *Una copia che si vede quasi mai è una copia che non viene mai corretta:* quel ramo scatta solo se la chiave manca da un file che si è caricato, quindi la divergenza non poteva essere notata da nessuno. Tolta la copia. | titolo: `Giro A del passo 18: i testi su richiesta escono dal codice` |
| 2026-09-15 | ⚠️ **Il censimento delle attese aveva un punto cieco, ed era VUOTO fino a oggi.** `conta-attese.js` ricava i file da censire dalla riga `FILES` di `run_full_regression.sh` — cioè dai file che la suite **lancia**. Un'attesa che vive in un **modulo condiviso** (`attese.js`, `quiz-driver.js`, `map-driver.js`) gira a ogni corsa e non entrava in nessun conto. Misurato prima di toccarlo: nei moduli condivisi c'erano **zero** `waitForTimeout`, quindi il punto cieco non aveva mai mentito a nessuno. *Un punto cieco vuoto non è un punto cieco chiuso: è lo stesso difetto che aspetta l'occasione.* L'occasione è stata questo passo — unificando `openModule` la sua attesa è finita in `map-driver.js`, e senza la correzione il documento avrebbe detto **268 con 269 attese vive**, cioè il numero sarebbe **migliorato** proprio mentre lo strumento smetteva di misurare. La lista dei moduli condivisi **non è scritta a mano**: si ricava dalle `require` dei file della suite, risolte accanto al file che le scrive — un elenco a mano è esattamente il difetto che questo strumento aveva già avuto una volta (`ATTESA_VERA`, tre nomi su otto). ⚠️ **Il caso `[F]` ha trovato un difetto vero appena scritto:** è uscito **rosso** perché la scoperta non veniva applicata ai file passati **a mano** — cioè proprio la strada che il test dello strumento percorre. Un'aggiunta valida solo sulla strada automatica sarebbe rimasta non misurata per sempre. | titolo: `Passo 16: 2004 righe morte via, openModule in un posto solo` |
| 2026-09-15 | ⚠️ **Un limite dichiarato di `test_blocco_ascolto.js` si appoggiava a un file che non lanciava nessuno.** La riga (regola 32) diceva che il caso non coperto — un modulo che smette di *chiamare* `renderListenBlock` — è preso da quattro test dei singoli moduli, e il quarto era `legacy/test_qm`, in `tests/legacy/`, **fuori dalla suite da settimane**. Misurato: dei quattro nominati, `test_batch16` copre Flash Card (`#fc-front-audio`), `test_batch17` Repeat Aloud (`#repeat-aloud-body`), `test_story_modules` Why We Say It (`.wws-bubble`) — e **Match Practice en→it, il blocco della consegna `#qm-prompt-audio`, era cercato SOLO dal file morto.** Cancellando la cartella quel buco non nasce: **si vede**. *Un limite dichiarato che si appoggia a un file morto è peggio di un limite non dichiarato: dice che qualcuno guarda, e non guarda nessuno.* La riga ora nomina i tre veri con il loro selettore e dichiara lo scoperto; **chiuderlo è scrivere un'asserzione nuova, quindi sta in `decisioni.md` con la sua condizione**, non qui. | titolo: `Passo 16: 2004 righe morte via, openModule in un posto solo` |
| 2026-09-15 | **`tests/legacy/` e `tests/debug/` cancellate: 14 file, 2004 righe che nessuno lanciava.** Il README di ciascuna dichiarava la condizione per cui erano tenute — *«se un giorno risulta che coprono qualcosa che i `test_batchN.js` non coprono, è meglio poterlo verificare piuttosto che doverlo riscrivere da zero»* — e in settimane nessuno l'ha verificata. ⚠️ **Misurato il 15 settembre, non poteva esserlo:** `legacy/test_retouch.js` asserisce che il pulsante Indietro di Flash Card è raggiungibile e abilitato, e `legacy/test_fc_navremoval.js`, **nella stessa cartella**, asserisce che è stato tolto dal DOM. `#fc-prev-btn`, `#fc-next-btn` e `#fc-nav-row` **non esistono in `index.html`**: uno dei due file era **rosso garantito**. Due file tenuti per poterli rilanciare, di cui uno non poteva passare. *E il costo non è stato zero: il 2026-09-09 due rinomine di passaggio hanno attraversato tutte e due le cartelle — duemila righe morte che hanno ricevuto manutenzione senza restituire niente.* Il conto reale ha smentito il censimento della catena, che diceva «6 file + 6 file»: sono **5 `.js` + README** in `legacy/` e **9 `.js` + README** in `debug/`. | titolo: `Passo 16: 2004 righe morte via, openModule in un posto solo` |
| 2026-09-15 | **`openModule` era ricopiato in 23 file di test, identico in tutti e 23.** Verificato **per contenuto** e non a occhio (hash di ogni definizione: 23 copie, **1 sola variante**) — che è la misura che conta: *la domanda non è quante copie, è quante versioni diverse.* Ora sta in `tests/map-driver.js`, accanto a `quiz-driver.js` e `story-driver.js`, con lo stesso ruolo per la mappa. **Non è finito in `module-order.js`**, che sarebbe stato il posto più comodo: quel file non tocca mai `page`, legge `index.html` da disco, e aggiungerci una funzione che pilota il browser ne cambierebbe la natura. −115 righe, +23. ⚠️ **L'attesa dentro NON è stata convertita, ed è dichiarato in tre posti** (nel file, in `tests/README.md`, nel documento generato): questo passo **sposta**, non paga debito. Il censimento scende da **291 a 269** navigazioni — 22 righe sparite perché erano copie, non perché qualcuno le ha sistemate. *Chi guarderà la serie dei numeri fra un mese deve poter distinguere questo calo da un lavoro vero.* | titolo: `Passo 16: 2004 righe morte via, openModule in un posto solo` |
| 2026-09-15 | **Un commento di `index.html` mandava a cercare un dato nel posto sbagliato, due righe sopra la chiave che lo smentisce.** Sopra `CONFIG.grades` c'era scritto che *«il nome esteso di ciascun grado (`levels.A.label`) sta nel file episodio, non qui»* — ma il nome che lo studente legge lo dà `CONFIG.gradeNames`, **due righe sotto**, e il campo `levels.X.label` nei file dati non lo legge nessuno (`episodeGrade()` prende `.items` e basta). Lo stesso commento citava il percorso `data/{livello}-episodioN-{lingua}.json`, nomenclatura morta dal passo 6. *Un commento che manca costa meno di un commento che manda a cercare un dato vivo nel posto sbagliato.* Il campo nei dati resta: la regola 4 lo richiede. | titolo: `Passo 16: 2004 righe morte via, openModule in un posto solo` |
| 2026-09-15 | **`toccaFinoA` diceva «esaurite le mosse» senza dire DOVE erano finite** — e la CI rossa su `[QM Task1]` l'ha mostrato: `53/53`, e nient'altro. Il ciclo ha **cinque diramazioni che consumano una mossa senza mai toccare un'opzione** (popup, ripasso, riquadro aperto, ultima domanda), quindi «esaurite» può voler dire due cose opposte: cinquantatré tentativi andati male, oppure **zero tentativi**. Ora stampa la ripartizione (`{popup, ripasso, riquadro, ultimaDomanda, tocchi}`) e **si ferma dopo dodici mosse di fila senza toccare un'opzione**, nominando la diramazione. Controprova col riquadro che non si chiude: **13 mosse invece di 53**, `{riquadro: 12, tocchi: 1}`. ⚠️ **Il posto del controllo è stato la correzione:** la prima versione stava in mezzo al ciclo e non è mai scattata, perché ogni diramazione fa `continue` e la salta — misurato, non dedotto. | titolo: `toccaFinoA dice dove sono finite le mosse, e si ferma quando gira a vuoto` |
| 2026-09-15 | ⚠️ **`sound.events.corretto` non ha una chiave `volume`, e un'etichetta di test citava un numero che non esiste nella fonte.** `test_dialogo_extra.js` diceva *«il volume del countdown è più basso di Corretto/Sbagliato default (0.15)»*: falso due volte — `sbagliato.volume` è **0.22**, e **0.15 non compare in `APP_CONFIG` da nessuna parte**. È il default scritto dentro `sfxPlayTone` (`volume === undefined ? 0.15 : volume`), cioè **un valore modificabile fisso nel codice: violazione della regola 3**. Trovato convertendo il passo 15, perché leggere la fonte ha fatto uscire `undefined` dove l'etichetta prometteva un numero. Il test ora dice la verità (confronta col default di `sfxPlayTone`, nominandolo); **il valore NON è stato spostato in `APP_CONFIG`: è un lavoro sull'app, fuori dal passo 15,** ed è registrato in `decisioni.md`. | titolo: `Passo 15: 39 righe, non quattro — e i colori diventano una guardia` |
| 2026-09-15 | **I cinque colori d'accento erano in TRE posti, e il test era il terzo.** Le altre due sono entrambe nell'app — `--accent` nel CSS e `APP_CONFIG.themes.options[].dot` — e **nessuno dei due legge l'altro**. Il test ora legge `CONFIG` e confronta con il CSS: smette di essere una copia e diventa **la guardia contro le altre due**, che prima potevano divergere in silenzio. Provato cambiando una cifra in `themes.options[1].dot`: rosso. Con la forma vecchia sarebbe stato verde, perché non nominava `themes.options`. ⚠️ Il confronto ignora maiuscole/minuscole di proposito: in `CONFIG` quattro colori sono maiuscoli e uno no, e **quell'incoerenza è la firma della copia fatta a mano** — uniformarla cancellerebbe la prova senza togliere la duplicazione. | titolo: `Passo 15: 39 righe, non quattro — e i colori diventano una guardia` |
| 2026-09-11 | ⚠️ **`test_batch16.js` [Job8] poteva passare provando la cosa sbagliata.** Il blocco apre **due moduli** (Voice Coach, poi Flash Card) e confronta gli stili dei due contatori, con 200 ms di guardia dopo ogni apertura — che è asincrona. Misurato: leggendo `#fc-counter` **mentre si è ancora su Voice Coach** i valori sono **identici** a `#vc-counter` (`sr-counter`, `IBM Plex Mono`, `700`, `18.4px`), perché gli elementi delle viste non attive restano nel DOM e `getComputedStyle` risponde lo stesso. **Un'apertura lenta non avrebbe prodotto un rosso: avrebbe prodotto un verde che prova che un contatore è uguale a sé stesso.** Le due viste ora si aspettano **una per una** (`#view-voice-coach`, poi `#view-map`, poi `#view-flashcard`): un approdo generico *«una vista diversa dalla mappa»* sarebbe già vero per la prima. | titolo: `14b chiuso: tre gesti letti da cinque angoli, 186 → 0` |
| 2026-09-11 | ⚠️ **Una seconda asserzione VACUA, e questa volta con l'indirizzo di dove nascono.** `test_batch7.js` chiudeva il giro di Flash Card con `log('[Job3/4] Flash Card reachable/answerable (sanity …)', true)` — **passava sempre**. Non era distrazione: il ciclo risponde *«Sì, la so»* a ogni carta, quindi la valvola di sicurezza (che si apre dopo ripetuti sbagli) **non poteva aprirsi mai**, e l'asserzione che l'autore voleva scrivere sarebbe stata rossa. Sostituita da due asserzioni vere — *«almeno una carta è stata risposta davvero»* (12, stampate accanto) e *«il giro arriva a uno stato noto invece di restare fermo su una carta»* — scritte **prima** di togliere il `true`. **Controprova su un guasto vero dell'app** (mazzo di Flash Card vuoto): tutte e due rosse; col `true` al suo posto quel guasto sarebbe passato 11 su 11. ⚠️ **E il dove:** entrambe le vacue trovate finora sono comparse dentro il *sanity check* di un ciclo — scritto in `tests/ERRORI-INGOIATI.md` come famiglia **⓪-ter**, perché *un difetto con un indirizzo si cerca, uno senza si aspetta*. | titolo: `«altro»: le tre trovate, le marcature, le quattro conversioni` |
| 2026-09-11 | **`test_batch16.js`: un'asserzione vera ovunque, scritta in un posto che mentiva.** Verificava che `#fc-level-label` «non esistesse più» **dopo** aver aperto Flash Card e atteso 200 ms — ma quell'id non esiste in nessun punto del documento (nell'app sopravvive solo dentro un **commento**), quindi la riga era vera anche sulla schermata iniziale. Non era inutile — se qualcuno rimettesse quell'id cadrebbe — ma faceva credere di provare qualcosa dentro Flash Card, e si portava dietro un'attesa che non guardava niente. **Spostata prima dell'apertura del modulo e rinominata** (*«nessun elemento con id fc-level-label esiste nel documento»*): il posto era la correzione. | titolo: `«altro»: le tre trovate, le marcature, le quattro conversioni` |
| 2026-09-11 | **`test_batch5.js`: una motivazione che lo strumento non sapeva leggere.** Il file portava già **cinque righe di prosa** sul perché quell'attesa dovesse restare a tempo, e chi le ha scritte pensava di aver chiuso la questione — ma `conta-attese.js` legge il marcatore `ATTESA-LEGITTIMA`, non i commenti: quel punto tornava nel conto del debito a ogni giro. Aggiunto il marcatore, accanto alla prosa, con scritto perché non bastava. *Una spiegazione che lo strumento non sa leggere è una spiegazione che va riscritta ogni volta.* | titolo: `«altro»: le tre trovate, le marcature, le quattro conversioni` |
| 2026-09-11 | **`test_batch15.js` leggeva `#repeat-aloud-type-badge` senza nessuna guardia**, subito dopo un `openModuleFromMap` che è **asincrono** (`Promise.all([...]).then(...)`). Il badge **nasce vuoto** — misurato campionando ogni 5 ms — e si riempie con l'attivazione della vista: su una macchina più lenta il `$eval` trovava la stringa vuota e `indexOf('Studio') === 0` falliva. Non era una guardia a tempo da convertire: era una **corsa scoperta**, trovata leggendo il codice per il triage della famiglia ⑤. | titolo: `14b ⑤: la famiglia del testo, e la settima categoria` |
| 2026-09-11 | ⚠️ **`[SR Task1]` di `test_batch19.js` è andato rosso in CI e verde in locale, e il log non conteneva niente con cui capirlo.** `toccaFinoA` torna `null` per **cinque** ragioni diverse — riepilogo raggiunto, schermata sbagliata, opzioni sparite, contatore illeggibile, budget di mosse esaurito — e l'unica asserzione che la legge ne riporta sempre una sola. **Due ipotesi costruite leggendo il codice sono state MISURATE e sono cadute tutte e due**: col countdown a 1 secondo il blocco resta verde (40/40, e del resto il test alza già `timeLimitSeconds` a 30 apposta), e schermata/opzioni/contatore compaiono nello stesso istante (35 ms misurati), quindi il `waitForFunction` li copre già. Restava da inventare una terza ipotesi — e inventarla sarebbe stato il difetto, non la correzione. Ora la funzione **si arrende dicendo quale delle cinque**, con mosse usate su budget e il totale letto alla partenza, ed è la stessa lezione di `attendi.sh` (uscita 2 «è vivo» contro 3 «è morto»). ⚠️ **Segnala anche un difetto latente: se il contatore non si legge, `totale` è `null` e il budget crolla da ~30 mosse a 8 senza che niente lo dica.** ⚠️ **LA CAUSA PROFONDA NON È TROVATA**: questo commit non la chiude, rende il prossimo rosso leggibile. Controprova: forzando il budget a 1 la firma riprodotta è identica a quella della CI, le stesse due rosse e l'asserzione condizionata che non parte. | titolo: `toccaFinoA si arrende dicendo quale delle cinque` |
| 2026-09-11 | ⚠️ **`tests/tools/attendi-ci.sh` diceva «il push non ha fatto partire la CI» su una corsa che stava girando.** Il parametro `head_sha` dell'API di GitHub confronta la stringa **intera** e non fa match sui prefissi: passandogli un commit abbreviato la risposta è `total_count: 0`, cioè **lo stesso identico corpo di una corsa che non esiste**. Misurato nello stesso minuto, sullo stesso commit: `head_sha=16dd7bd` → 0, `head_sha=16dd7bda0f…` → 1. Lo strumento nato per non improvvisare la lettura della CI (N.12) ha prodotto il **quarto** difetto della stessa famiglia — e il peggiore dei quattro, perché **la risposta falsa era azionabile**: non «non ho capito», ma «rifai il push». Ora il commit si estende con `git rev-parse` e, se resta più corto di 40 caratteri, l'uscita è **64 con il motivo**, mai un 3. ⚠️ **E il test non l'aveva preso perché passava un commit di sette caratteri: il curl finto risponde la stessa cosa qualunque URL gli si dia, quindi la domanda sbagliata non si vedeva** (regola 42 — il caso comodo). Ora i casi usano un commit di 40 caratteri, il lancio avviene fuori dal repository perché `git rev-parse` non risolva per caso un prefisso vero, l'harness legge anche **stderr** (gli errori d'uso parlano lì), e c'è il caso `[G]`: senza la correzione va rosso e risponde 3. | titolo: `attendi-ci.sh chiedeva all'API una domanda a cui non sapeva rispondere` |
| 2026-09-11 | **`tests/suite.log` non era in `.gitignore`.** È il log della corsa completa — prodotto a ogni giro come `tests/server.log` e i `*.result.txt`, che lì dentro c'erano già — quindi a ogni verifica restava come file non tracciato e chiedeva di essere committato. Aggiunto accanto agli altri due. *Toccare `.gitignore` a suite aperta è un'eccezione alla regola 36, e non presa a intuito: misurato prima, **zero file della suite e dell'app lo leggono**, quindi la modifica non può contaminare nessun risultato.* | titolo: `tests/suite.log fra i prodotti di una corsa` |
| 2026-09-11 | ⚠️ **`tests/tools/conta-attese.js` contava come debito nuovo l'effetto del lavoro che il debito lo stava togliendo.** L'elenco delle «attese vere» era **scritto a mano** e fermo ai tre helper che esistevano quando lo strumento è nato; nel frattempo ne erano arrivati cinque (`attendiAbilitato`, `attendiDisabilitato`, `attendiClasse`, `attendiCheParla`, `attendiTono`). Risultato: **ogni `waitForTimeout` che stava PRIMA di una conversione veniva promosso da navigazione a guardia**. Il conto diceva **79 dove il vero era 56**, e i numeri con cui si era pianificata l'intera giornata (154, 108, 90) erano gonfiati — *il 186 del 14a invece è giusto, nessuna di quelle funzioni esisteva ancora*. È la regola 37 applicata allo strumento di misura: **una misura che non misura, e non lo dice.** Corretto leggendo `module.exports` di `attese.js` invece di elencarli — una funzione nuova è riconosciuta il giorno stesso. `test_conta_attese.js` ha ora un blocco **generato dagli export**, uno per funzione: con l'elenco a mano rimesso al suo posto il test va rosso (3 guardie diventano 8). | titolo: `14b ④: la famiglia del localStorage, e lo strumento che contava male` |
| 2026-09-11 | **`test_voicecoach.js`: un'etichetta che prometteva più di quanto l'asserzione controllasse.** La riga diceva *«Clicking it marks voiceCoach completed **and returns to the map**»*, ma l'asserzione leggeva **solo** il completamento: il ritorno alla mappa era dichiarato e non verificato da nessuno. Chi leggeva il nome del test credeva che quel comportamento fosse protetto — è la regola 32 vista da un'altra porta. Spezzata in due asserzioni: ora il ritorno alla mappa è **provato**, non dichiarato. *Trovato leggendo il codice per il triage della famiglia ④, non prodotto dalla conversione.* | titolo: `14b ④: la famiglia del localStorage, e lo strumento che contava male` |
| 2026-09-10 | **`tests/tools/attendi.sh` non si arrendeva davvero: aveva un tetto solo, tarato a 90 minuti su una suite di 17, e quando parlava dava l'informazione sbagliata.** Su un lavoro morto diceva «tempo scaduto» — cioè *«ho aspettato troppo»* invece di *«ho aspettato un cadavere»*. Ora ha **due rilevatori**, perché sono due guasti: il **tetto** vede il lavoro vivo che non finisce (uscita 2), il **silenzio sul log** vede il lavoro morto (uscita 3, con due frasi distinte per «ha scritto e poi ha smesso» e «non ha mai scritto niente — controlla il percorso»). Gli argomenti sbagliati escono con **64** e non più con lo stesso 2 del tempo scaduto. La soglia di silenzio (600 s) viene da una misura, non da un'intuizione: la forma di `run_full_regression.sh` rende il silenzio massimo legittimo pari alla durata del file più lento — **192 s** il 2026-09-10, su 1019 s di suite. | titolo: `attendi.sh si arrende da sola, e dice quale dei due guasti ha davanti` |
| 2026-09-08 | **I diciannove punti di `test_batch19.js` erano una specie, non un esemplare.** Delle 28 attese fisse del file, 19 facevano da guardia a un'asserzione, e il rosso in CI cadeva su una riga diversa a ogni corsa perché **il test non controllava lo stato che misurava**. Riscritto **tutto il gruppo** — `[QM Task1]`, `[SR Task1]`, `[SR Task3]`, `[SR Task3-adj]` — con tre funzioni condivise (`statoQuiz`, `toccaFinoA`, `attendiDomandaSuccessiva`) che servono sia Match Practice sia Speed Match: un posto solo da correggere invece di quattro. Il file è sceso da 28 attese a 17. *(La riga stava in `decisioni.md` con la condizione «alla prossima apertura di quel file, e si riscrive tutto il gruppo»: eseguita dal passo 0a-bis, chiusa qui l'11 settembre — era rimasta aperta a torto.)* | `31d6586` e precedenti (passo 0a-bis) |
| 2026-09-07 | `test_batch19.js`: i quattro cicli che cliccano sempre la prima opzione — uno in Match Practice, tre in Speed Match — provavano **venti** giri su un grado che ha **nove** domande. Se la fortuna andava storta per tutte e nove, il giro finiva, le opzioni sparivano, e il click successivo restava appeso trenta secondi su una schermata di riepilogo prima di morire con un `TimeoutError` che non diceva niente. Ora `tapPrimaOpzione()` si accorge che non c'è più niente da toccare e il ciclo esce: l'asserzione «Managed to observe...» subito dopo diventa il rosso che **spiega**. | titolo: `Il ciclo di Speed Match sapeva contare fino a venti su nove domande` |

| 2026-09-17 | **Il fatto sulla CI stava nel file GENERATO invece che nel generatore, e il generatore l'ha cancellato.** Il 16/09 avevo scritto in testa a `BASELINE-ASSERZIONI.txt` che l'ultimo commit di ogni giro che aggiorna quel file è coperto dalla CI e non dalla suite locale. `conta-asserzioni.js --scrivi` **rigenera l'intestazione da zero**: al primo aggiornamento successivo il blocco è sparito, senza errore e senza un diff che qualcuno guardi — il diff di un baseline si legge per i numeri, non per i commenti. Corretto spostando il fatto **dentro `tests/tools/conta-asserzioni.js`**, e aggiungendo all'intestazione generata una riga che dice **di essere generata**, così chi arriva non ci scrive dentro a mano. ⚠️ **E la prova che la correzione funziona non è che sembri giusta: è che il giorno dopo ha retto.** Il 17/09 un secondo `--scrivi` (baseline 1254 → 1263) **ha lasciato il testo al suo posto**. *Se l'avessi solo riscritto a mano, oggi sarebbe sparito una seconda volta e non me ne sarei accorto.* **Il posto per una cosa che deve durare è il modello, non il prodotto** — e vale oltre questo file: ogni intestazione rigenerata da uno strumento ha la stessa proprietà. | titolo: `Il fatto sulla CI va nel generatore, non nel file generato` + `Baseline asserzioni 1254 -> 1263` |

| 2026-09-17 | **Un'asserzione del 21-bis è diventata rossa per una DECISIONE, non per una regressione — e la correzione non è stata toglierla: è stata seguirla dove la cosa che protegge è andata a stare.** `test_pulizie_registrate.js` verificava che `moduleEpoch++` stesse **prima** di `stopAllModuleActivity` **dentro `showView`**; la prima estrazione del passo 22 l'ha spostato in `leaveModule`. *L'invariante non è cambiato di una virgola — l'incremento dell'epoca deve precedere la pulizia, o un callback che arriva mentre le pulizie girano troverebbe l'epoca vecchia e si crederebbe ancora valido — è cambiato dove vive.* Aggiunta **l'altra metà, che prima non aveva niente a proteggerla**: `showView` non deve più contenere né l'incremento né la pulizia. Senza quella riga, rimettercene uno dentro sarebbe passato verde e le due chiamate pre-login avrebbero ripreso a trascinarsi tre strati. ⚠️ **E un difetto della correzione stessa, trovato subito:** il corpo della funzione veniva ritagliato con una finestra fissa di 400 caratteri — esatta finché `showView` era sola, e **sconfinante dentro `leaveModule`** dopo la separazione, cioè l'asserzione nuova nasceva rossa su codice giusto. Ora il taglio chiede al codice dove finisce la funzione (`corpoDi`). *Un numero fisso era esatto per il codice di ieri e falso per quello di oggi senza cambiare una cifra: la stessa forma dei tempi citati invece che rimisurati (regola 38).* | titolo: `L'asserzione sull'epoca segue moduleEpoch in leaveModule` |

| 2026-09-17 | **Due correzioni che NON servono allo strato in corso: servono ai sei che vengono dopo** — e stanno in un commit loro apposta, perché dentro quello di un'estrazione sembrerebbero parte di essa. ⚠️ **① `regioneDiRicerca()` di `test_config_letta.js` era un elenco di file scritto a mano** (`app/config.js` + `index.html` + i `.json`), e **ogni strato estratto porta fuori dei lettori di CONFIG**. Misurato appena estratto il primo: `app/avvio.js` legge `APP_CONFIG.themes.defaultTheme` ed era **già fuori dalla regione**; il test era verde per un pelo, perché quella chiave è nominata altre tre volte in `index.html`. Alla prima chiave nominata *solo* dentro un file estratto, il test avrebbe detto «chiave morta» su una chiave viva — **non un verde che mente, ma un rosso falso per strato, cioè sei diagnosi pagate**. Ora la regione si deriva da `app/*.js` e **cresce da sola: chi estrae uno strato non deve sapere che quel test esiste** (terza applicazione della mossa di `[C]` e `[E]`). **Falsificato sul guasto che VERRÀ, non su quello che c'è:** tolto `defaultTheme` da `index.html` lasciandolo solo in `app/avvio.js`, la forma **nuova resta verde** e la **vecchia diventa rossa**. ⚠️ **② La descrizione di `[D]` in `test_avvio_invariato.js` la scrive ora la fotografia.** Diceva «4 (2 esterni + 2 inline)», poi «4 (3 esterni + 1 inline)»: **ogni estrazione sposta un pezzo da inline a esterno, quindi ogni descrizione che conta i due gruppi diventa falsa senza che il numero cambi** — resta verde e falsa, e nessuna corsa può accorgersene. *Stessa forma dei tempi citati invece che rimisurati (regola 38) e della finestra fissa di 400 caratteri.* Il **conto atteso** resta scritto a mano, perché quello deve essere una decisione; la **composizione** si legge da ciò che si è appena misurato. | titolo: `Due correzioni per i sei strati che vengono dopo` |

## Regole di lavoro (`CLAUDE.md`)

| 2026-09-11 | **Regola 44 — l'effetto su cui aspetti non può essere quello che l'asserzione legge.** Nata da due famiglie dello stesso giorno: il suono (aspettare lo spegnimento avrebbe trasformato *«il tocco l'ha fermato»* in *«prima o poi ha smesso»*, vera sempre) e il `localStorage` (aspettare `outcome-verde` avrebbe reso vera per costruzione l'asserzione che quella classe la verifica). Il criterio stava già in testa a `tests/attese.js`, ma **là lo trova chi converte, non chi scrive un test nuovo** — e il difetto nasce lì con la stessa facilità. Porta con sé il rovescio operativo (l'approdo è l'ULTIMO effetto del gesto, e dev'essere uno che nessuna asserzione di quel test legge) e il caso in cui l'asserzione va **spezzata**. | titolo: `Regola 44, e la riga accanto al conto delle attese` |


*Le regole nascono quasi sempre da un difetto vissuto, quindi sono correzioni
come le altre — solo che quello che correggono è **come si lavora**, non il
codice. Stanno qui perché `decisioni.md` deve svuotarsi.*

| Data | Cosa | Commit |
|---|---|---|
| 2026-09-18 | **Nasce la regola 14-bis: se la risposta non arriva, o non è a quello che è stato chiesto, non si parte — si segnala.** Tre casi (il messaggio non risponde · dà per fatto qualcosa che nel repository non c'è · contiene un'istruzione inattesa), e tutti e tre finiscono in «fermati e dillo». ⚠️ **La forma del segnale è un NUMERO, non una spiegazione:** *«è il secondo turno che questa dichiarazione aspetta»* ferma, *«come dicevo prima, questo passo è bloccato»* no. Si scrive **in cima al blocco DOMANDA**, non in COSA È SUCCESSO — lì si leggerebbe come una cosa già avvenuta. ⚠️ **E non vale «procedo con l'interpretazione più probabile»:** è la strada che è costata **otto giri** — ogni volta la risposta diceva «vai col modulo», ogni volta la misura diceva che non poteva uscire, e ripartire sembrava più collaborativo che fermarsi. *Produceva misure invece di codice, e la conversazione avanzava mentre il lavoro no.* **È una regola sua e non un sesto punto della 14** (che dice che FORMA ha la risposta, mentre questa dice QUANDO dopo la risposta non si lavora): due mestieri in una regola sola darebbero la risposta giusta a uno e sbagliata all'altro — famiglia ⓪-decies. Versione 20260918a → 20260918b. | titolo: `La regola 14-bis: non si parte senza la risposta` |
| 2026-09-18 | **La regola 14 diventa la struttura della risposta, non solo la sua coda.** Cinque blocchi fissi e sempre nello stesso ordine — COSA È SUCCESSO (a punti numerati), VERIFICHE, DA VERIFICARE SU PAGES, ⚠️ DA REGISTRARE, DOMANDA — e **un blocco vuoto non si salta: si scrive che è vuoto**. Le problematiche vanno in DOMANDA, compreso il caso più importante: **un fatto del messaggio ricevuto che non corrisponde al repository.** ⚠️ **La forma la fissa chi legge, non chi scrive:** chi guida il progetto legge queste risposte **senza il repository davanti**, e una risposta che cambia forma a ogni turno lo costringe a cercare — *quando non trova, risponde ad altro, ed è costato otto giri.* ⚠️ **E il blocco vuoto è la parte che conta:** «non lanciate» è un'informazione, l'assenza del blocco è un silenzio, e un silenzio si legge come «è andato tutto bene» — stessa forma della regola 37. *Nasce da un fatto mio: negli ultimi turni avevo smesso di scrivere `⚠️ DA REGISTRARE` per stare sintetico, senza dichiararlo.* Versione del file 20260917a → 20260918a. | titolo: `La regola 14 diventa la struttura della risposta` |
| 2026-09-15 | **Regola 38 — il tempo della CI portava «undici minuti», esatto al 2026-09-07 su una suite di 42 file e 933 asserzioni, falso oggi su 49 e 1118.** Il 15 settembre l'ho letto da lì invece che dall'elenco delle corse su GitHub e ci ho costruito sopra una conclusione falsa (*«il runner ha meno core del container»*): le corse vere dicevano 17,5 min stabili e poi 9,2, cioè **1,90× — lo stesso 1,94× misurato in locale**. Adesso la regola porta i due tempi **con la data**, dice che le durate vere stanno nell'elenco delle corse — che si aggiorna da solo — e che **un tempo non si cita, si rimisura**. Stessa correzione sull'invariante «venti suite invece di un macello» di `decisioni.md`, che diceva «undici minuti l'una» e adesso dice 5,4 con la data. ⚠️ *Un numero scritto in un documento non cresce con la cosa che misura: resta esatto per la domanda di allora e diventa falso per quella di adesso senza cambiare una lettera.* | titolo: `I tempi di CI e suite portano la data, e si rimisurano` |
| 2026-09-11 | **Regola 43 — ogni commit che non è un passo della catena scrive la propria riga, nello stesso commit**, e la verifica è sul **diff**, non sul messaggio: *un commit che tocca codice o test e non tocca un registro è un commit che non ha registrato niente*. Nasce dai quindici commit del 2026-09-10: `decisioni.md` toccato **12 volte su 15**, `correzioni.md` **una**. Il buco non era «registrare» ma registrare **quello che era stato corretto**, e il giorno dopo è servito un giro di allineamento. ⚠️ La prima forma del controllo guardava il messaggio e **dava otto falsi allarmi su quindici** — misurata, non intuita, e scartata per questo. | titolo: `Regola 43: la riga nasce col commit, e la verifica è sul diff` |
| 2026-09-08 | **Regola 40 — un lavoro a più passi sta sempre nella lista attività**, un elemento per passo, col progresso vero (`Suite 15/42`) aggiornato ogni paio di minuti. *Il pallino che gira è un'animazione, non una misura: da solo non distingue «sta lavorando» da «si è fermato».* È la prima cosa del progetto che chi guida può verificare **senza chiedermela**. *(La riga stava in `decisioni.md` con la condizione «va in `CLAUDE.md` come regola permanente»: eseguita, e chiusa qui l'11 settembre — era rimasta aperta a torto.)* | `b314924` *(entrata insieme al passo 6)* |

## Contenuto dell'episodio

| Data | Cosa | Commit |
|---|---|---|
| 2026-09-07 | `data/inglese/it/inglese-it-gate.json` riscritto da `docs/inglese/it/inglese-it-gate.md`: i numeri dichiarati nella fonte (15 voci in A, 7 in B, 9 in C, 9 battute in D, 8 skill, 8 slot) non corrispondevano al file dati (16, 7, 10, 9), quindi l'app mostrava un contenuto diverso da quello deciso. Il grado C non ricopia più le battute a mano: dove la fonte scrive `= dN` la frase viene dalla battuta, così non possono divergere. `episodeTitle` da "Presentarsi" ad "Al gate", ed etichetta del personaggio da "Guida" a "Hostess al gate". | titolo: `Il file episodio riscritto dalla sua fonte` |

## Why We Say It

| Data | Cosa | Commit |
|---|---|---|
| 2026-09-04 | Dodici correzioni in un giro: traduzione dentro la bolla e sempre visibile (via il link "Mostra traduzione"); Blocco Ascolto dentro la bolla; bordo e sfondo delle card più marcati; nessuno dei tre pulsanti preselezionato; il pulsante scelto blu accento e non verde; la risposta si può cambiare anche al primo giro; via l'etichetta accanto alla spunta ("✓ NON CHIARA"); le card senza regola prendono la spunta quando la sequenza le supera; venti frasi di supporto per risposta, mai ripetute dentro lo stesso modulo. | `b3f9a4a` |
| 2026-09-04 | A 320 e 360 px "Esci e riprendi dopo" usciva dallo schermo (`.se-complete-row .btn`: ora va a capo dentro il pulsante invece di allargarlo). | `b3f9a4a` |

| 2026-09-07 | Sblocco Sequenziale, variante per dichiarazione: una card `is-ahead` non risponde più a niente. Prima l'attenuazione era solo grafica e il Blocco Ascolto dentro la card restava cliccabile — l'audio partiva da una card futura, aggirando la sequenza che il meccanismo esiste per imporre. Guardia sola in cima al gestore, stessa forma della variante per ascolto nel Dialogo. | `86e7462` |

## Dialogue (Repeat in Time, Real Dialogue)

| Data | Cosa | Commit |
|---|---|---|
| 2026-09-04 | L'area sensibile per passare alla battuta successiva mentre scorre la barra è **tutta la bolla**, non la sola barra del tempo. Vale per entrambi i profili. Il pulsante "Prossima frase" resta. | `b3f9a4a` |
| 2026-09-04 | La Spiegazione di entrambi i moduli dice che si può anche toccare la battuta per andare avanti. | `b3f9a4a` |

| 2026-09-07 | Real Dialogue: toccare una battuta mentre parla ne salta l'audio, come negli altri due profili. Il salto stava **dopo** la guardia `advance === 'auto'`, che usciva prima: una perdita causata dall'ordine delle righe, non una scelta. | `86e7462` |

## Voice Practice / Voice Check

| Data | Cosa | Commit |
|---|---|---|
| 2026-09-07 | Premere il microfono e non parlare ora **conta** come registrazione senza parole: il ramo del timeout di silenzio incrementa `vcEmptyRecognitionStreak` e aggiorna l'avviso. Prima quel contatore si muoveva solo dentro `vcEvaluate()`, cioè dopo un "Invia" — e una registrazione muta viene scartata prima di arrivarci, quindi il gesto più comune di chi ha il microfono rotto era l'unico che all'avviso non arrivava mai. Non erano due meccanismi in competizione: era uno che non veniva mai innescato. Livello 1 dopo due tentativi. Il pannello è uscito da `#vc-result`, che nello stato di riposo è nascosto: da lì dentro sarebbe salito di livello restando invisibile. Quando l'avviso c'è, la riga sotto il microfono si toglie invece di ripetere lo stesso concetto. | titolo: `Premere e non parlare conta come registrazione vuota` |

## Speed Match

| Data | Cosa | Commit |
|---|---|---|
| 2026-09-05 | Tolto `CONFIG.speedRound.pointsPerCorrect`: parametro presente nel Pannello Admin e mai letto da nessuno — una manopola che non muoveva niente (decisione D4). | `4b32777` |
