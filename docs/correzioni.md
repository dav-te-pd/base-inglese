# Correzioni

Il registro di cosa è stato corretto, diviso per modulo. Una riga per
correzione: **data, cosa, commit**.

**Prima di correggere qualcosa, cercalo qui.** Se risulta già corretto,
fermati e dillo, con data e commit — non rifarlo. Una correzione che si
ripresenta non è una correzione: è il sintomo che la prima non ha tenuto, o
che è stata scavalcata da una modifica successiva. Rifarla in silenzio
nasconde il problema vero.

Ogni correzione va aggiunta qui **nello stesso commit** che la applica, come
i test (regola 23). Una correzione registrata dopo è una correzione che, nel
frattempo, qualcun altro ha già rifatto.

Il *perché* di una scelta non sta qui: sta nel commento accanto al codice e
nel messaggio di commit. Qui sta il *cosa*, in una riga, per poterlo
ritrovare.

Nella colonna **Commit** sta l'hash quando la correzione è di un commit
precedente. Le righe aggiunte *insieme* alla correzione che descrivono portano
invece il **titolo** del commit: un commit non può contenere il proprio hash, e
mettercelo con una modifica successiva significherebbe scriverlo fuori dal
commit che lo giustifica. Il titolo si ritrova con `git log --grep`.

---

## Condiviso (mappa, esiti, configurazione)

| Data | Cosa | Commit |
|---|---|---|
| 2026-09-10 | **Il modulo si apriva prima di avere quello che gli serve.** `openVoiceCoach` disegnava la schermata e **abilitava il microfono** mentre la battuta non era ancora arrivata: premendolo in quella finestra, `tokenize(vcTargetText())` moriva con `Cannot read properties of null`. La causa non era «si apre presto»: era un **precaricamento travestito da dipendenza** — il `Promise.all` aspettava `loadFeedbackMessages()` e `results[1]` **non veniva mai letto**. Tolta quella riga la finestra passa da **17 ms di mediana a 1 ms**, e con la rete lenta simulata (2 s) il `TypeError` da 3 volte su 3 a nessuna. In più `openModuleFromMap` ora aspetta anche `loadEpisodeData`, che è il punto unico da cui passano tutti i moduli: **beneficio dichiarato teorico nel codice**, perché chi lo trova non lo tolga credendolo inutile. ⚠️ La finestra valeva per **tutti e otto** i moduli — misurato uno per uno — non per Voice Coach: gli altri sette accettavano un gesto che non faceva niente. Protetto da `tests/test_modulo_pronto.js`. | `c456b9d` |
| 2026-09-10 | **La guardia sa che Personalizza non legge un file episodio.** La riga aggiunta a `openModuleFromMap` (qui sopra) chiedeva `loadEpisodeData` a **tutti** i moduli, ma Personalizza è **l'unico dei sedici senza `dataFile`**: `fetch(undefined)` falliva, il `.catch` apriva `showLoadError`, e la schermata d'errore compariva su un modulo che funziona. La condizione si legge ora dal descrittore (`module.dataFile`), non da un elenco a parte da tenere allineato a mano.<br><br>⚠️ **E va scritta la parte scomoda: era una regressione introdotta e corretta nello stesso giro, e il buco stava nel test appena scritto.** `test_modulo_pronto.js` guidava **solo Voice Coach** — cioè il caso più simile a quello che stavo scrivendo, quello che avevo già montato e aperto. Cinque file rossi alla prima corsa (`test_batch2`, `test_batch3`, `test_batch3b`, `test_batch16`, `test_outcome_step_ids`). Il blocco `[C]` ora apre Personalizza dalla mappa. **Da qui la regola 42 di `CLAUDE.md`**: un test che guida un caso solo non protegge una riga che vale per tutti. | `a6e3f97` |
| 2026-09-05 | La regola di esito (`CONFIG.moduleOutcomeRules`) veniva cercata con l'**id del passo** invece che con l'id del **modulo**: dalla seconda apparizione in poi (`quickMatchEngIta-2`, `flashcardAEngIta-2`, `voicePractice-2`…) la chiave non esisteva, `saveModuleOutcome` non veniva chiamato e sei passi su ventidue restavano senza colore in mappa. Sette letture passate a `.moduleId` — un campo che l'app costruiva già su ogni passo e non leggeva nessuno. Era il § 4.1 di `docs/validazione.md`. | titolo: `La regola di esito arriva a ogni apparizione, non solo alla prima` |
| 2026-09-05 | `CONFIG.attemptRule` tolta. Le sue due righe (`voicePractice: 'lastAttempt'`, `voiceCoach: 'firstAttempt'`) duplicavano una distinzione già nel descrittore come `module.voiceVariant`, ed erano anch'esse indicizzate per id del passo: il passo 16 cadeva su `undefined` e usava il primo tentativo invece dell'ultimo. La regola vive ora in `vcEvaluate`, che rama su `vcVariant`. | titolo: `La regola di esito arriva a ogni apparizione, non solo alla prima` |

## Episodi di forma diversa (i tre difetti silenziosi)

*Tutti e tre chiusi insieme il 2026-09-08, perché sono la stessa famiglia: non
crollano, sembrano funzionare, e mordono al primo episodio che non ha la forma
del primo. Sono diventati urgenti con la decisione degli **episodi corti** —
solo gradi C e D.*

| Data | Cosa | Commit |
|---|---|---|
| 2026-09-08 | `openModuleByKind` aveva una catena di `if/else if` **senza ramo finale**: un passo con un `kind` sconosciuto era una riga cliccabile che non faceva nulla, restava "attuale" per sempre e bloccava tutti i successivi senza un errore in console. Ora il ramo finale mostra la schermata d'errore, che è già la strada per «qualcosa non va» (regola 35). | titolo: `Tre difetti silenziosi che aspettavano il primo episodio corto` |
| 2026-09-08 | `episodeGrade()` torna `[]` per un grado assente, e i costruttori di coda leggevano quel `[]` come «coda già finita»: il modulo si dichiarava **completato senza far fare un solo esercizio**, e registrava l'esito. Nasce `episodeGradeRequired()`, il lettore che **pretende**: sta nei sette punti dove il contenuto è indispensabile (cinque code e due schermate), alza, e il fallimento cade nella `.catch()` che ogni modulo ha già. `episodeGrade()` resta il lettore neutro per i gestori di click, dove «nessuna voce» vuol dire solo «niente da fare». | titolo: `Tre difetti silenziosi che aspettavano il primo episodio corto` |
| 2026-09-08 | `migrateCustomizeSeenToModuleProgress()` scriveva `'personalizzazione'` nei progressi di **qualunque** episodio, anche di uno che quel passo non lo dichiara. Ora controlla che l'episodio abbia davvero quel modulo. | titolo: `Tre difetti silenziosi che aspettavano il primo episodio corto` |

## Suite di regressione

| Data | Cosa | Commit |
|---|---|---|
| 2026-09-11 | ⚠️ **Una seconda asserzione VACUA, e questa volta con l'indirizzo di dove nascono.** `test_batch7.js` chiudeva il giro di Flash Card con `log('[Job3/4] Flash Card reachable/answerable (sanity …)', true)` — **passava sempre**. Non era distrazione: il ciclo risponde *«Sì, la so»* a ogni carta, quindi la valvola di sicurezza (che si apre dopo ripetuti sbagli) **non poteva aprirsi mai**, e l'asserzione che l'autore voleva scrivere sarebbe stata rossa. Sostituita da due asserzioni vere — *«almeno una carta è stata risposta davvero»* (12, stampate accanto) e *«il giro arriva a uno stato noto invece di restare fermo su una carta»* — scritte **prima** di togliere il `true`. **Controprova su un guasto vero dell'app** (mazzo di Flash Card vuoto): tutte e due rosse; col `true` al suo posto quel guasto sarebbe passato 11 su 11. ⚠️ **E il dove:** entrambe le vacue trovate finora sono comparse dentro il *sanity check* di un ciclo — scritto in `tests/ERRORI-INGOIATI.md` come famiglia **⓪-ter**, perché *un difetto con un indirizzo si cerca, uno senza si aspetta*. | titolo: `«altro»: le tre trovate, le marcature, le quattro conversioni` |
| 2026-09-11 | **`test_batch16.js`: un'asserzione vera ovunque, scritta in un posto che mentiva.** Verificava che `#fc-level-label` «non esistesse più» **dopo** aver aperto Flash Card e atteso 200 ms — ma quell'id non esiste in nessun punto del documento (nell'app sopravvive solo dentro un **commento**), quindi la riga era vera anche sulla schermata iniziale. Non era inutile — se qualcuno rimettesse quell'id cadrebbe — ma faceva credere di provare qualcosa dentro Flash Card, e si portava dietro un'attesa che non guardava niente. **Spostata prima dell'apertura del modulo e rinominata** (*«nessun elemento con id fc-level-label esiste nel documento»*): il posto era la correzione. | titolo: `«altro»: le tre trovate, le marcature, le quattro conversioni` |
| 2026-09-11 | **`test_batch5.js`: una motivazione che lo strumento non sapeva leggere.** Il file portava già **cinque righe di prosa** sul perché quell'attesa dovesse restare a tempo, e chi le ha scritte pensava di aver chiuso la questione — ma `conta-attese.js` legge il marcatore `ATTESA-LEGITTIMA`, non i commenti: quel punto tornava nel conto del debito a ogni giro. Aggiunto il marcatore, accanto alla prosa, con scritto perché non bastava. *Una spiegazione che lo strumento non sa leggere è una spiegazione che va riscritta ogni volta.* | titolo: `«altro»: le tre trovate, le marcature, le quattro conversioni` |
| 2026-09-11 | **`test_batch15.js` leggeva `#repeat-aloud-type-badge` senza nessuna guardia**, subito dopo un `openModuleFromMap` che è **asincrono** (`Promise.all([...]).then(...)`). Il badge **nasce vuoto** — misurato campionando ogni 5 ms — e si riempie con l'attivazione della vista: su una macchina più lenta il `$eval` trovava la stringa vuota e `indexOf('Studio') === 0` falliva. Non era una guardia a tempo da convertire: era una **corsa scoperta**, trovata leggendo il codice per il triage della famiglia ⑤. | titolo: `14b ⑤: la famiglia del testo, e la settima categoria` |
| 2026-09-11 | ⚠️ **`[SR Task1]` di `test_batch19.js` è andato rosso in CI e verde in locale, e il log non conteneva niente con cui capirlo.** `toccaFinoA` torna `null` per **cinque** ragioni diverse — riepilogo raggiunto, schermata sbagliata, opzioni sparite, contatore illeggibile, budget di mosse esaurito — e l'unica asserzione che la legge ne riporta sempre una sola. **Due ipotesi costruite leggendo il codice sono state MISURATE e sono cadute tutte e due**: col countdown a 1 secondo il blocco resta verde (40/40, e del resto il test alza già `timeLimitSeconds` a 30 apposta), e schermata/opzioni/contatore compaiono nello stesso istante (35 ms misurati), quindi il `waitForFunction` li copre già. Restava da inventare una terza ipotesi — e inventarla sarebbe stato il difetto, non la correzione. Ora la funzione **si arrende dicendo quale delle cinque**, con mosse usate su budget e il totale letto alla partenza, ed è la stessa lezione di `attendi.sh` (uscita 2 «è vivo» contro 3 «è morto»). ⚠️ **Segnala anche un difetto latente: se il contatore non si legge, `totale` è `null` e il budget crolla da ~30 mosse a 8 senza che niente lo dica.** ⚠️ **LA CAUSA PROFONDA NON È TROVATA**: questo commit non la chiude, rende il prossimo rosso leggibile. Controprova: forzando il budget a 1 la firma riprodotta è identica a quella della CI, le stesse due rosse e l'asserzione condizionata che non parte. | titolo: `toccaFinoA si arrende dicendo quale delle cinque` |
| 2026-09-11 | ⚠️ **`tests/tools/attendi-ci.sh` diceva «il push non ha fatto partire la CI» su una corsa che stava girando.** Il parametro `head_sha` dell'API di GitHub confronta la stringa **intera** e non fa match sui prefissi: passandogli un commit abbreviato la risposta è `total_count: 0`, cioè **lo stesso identico corpo di una corsa che non esiste**. Misurato nello stesso minuto, sullo stesso commit: `head_sha=16dd7bd` → 0, `head_sha=16dd7bda0f…` → 1. Lo strumento nato per non improvvisare la lettura della CI (N.12) ha prodotto il **quarto** difetto della stessa famiglia — e il peggiore dei quattro, perché **la risposta falsa era azionabile**: non «non ho capito», ma «rifai il push». Ora il commit si estende con `git rev-parse` e, se resta più corto di 40 caratteri, l'uscita è **64 con il motivo**, mai un 3. ⚠️ **E il test non l'aveva preso perché passava un commit di sette caratteri: il curl finto risponde la stessa cosa qualunque URL gli si dia, quindi la domanda sbagliata non si vedeva** (regola 42 — il caso comodo). Ora i casi usano un commit di 40 caratteri, il lancio avviene fuori dal repository perché `git rev-parse` non risolva per caso un prefisso vero, l'harness legge anche **stderr** (gli errori d'uso parlano lì), e c'è il caso `[G]`: senza la correzione va rosso e risponde 3. | titolo: `attendi-ci.sh chiedeva all'API una domanda a cui non sapeva rispondere` |
| 2026-09-11 | **`tests/suite.log` non era in `.gitignore`.** È il log della corsa completa — prodotto a ogni giro come `tests/server.log` e i `*.result.txt`, che lì dentro c'erano già — quindi a ogni verifica restava come file non tracciato e chiedeva di essere committato. Aggiunto accanto agli altri due. *Toccare `.gitignore` a suite aperta è un'eccezione alla regola 36, e non presa a intuito: misurato prima, **zero file della suite e dell'app lo leggono**, quindi la modifica non può contaminare nessun risultato.* | titolo: `tests/suite.log fra i prodotti di una corsa` |
| 2026-09-11 | ⚠️ **`tests/tools/conta-attese.js` contava come debito nuovo l'effetto del lavoro che il debito lo stava togliendo.** L'elenco delle «attese vere» era **scritto a mano** e fermo ai tre helper che esistevano quando lo strumento è nato; nel frattempo ne erano arrivati cinque (`attendiAbilitato`, `attendiDisabilitato`, `attendiClasse`, `attendiCheParla`, `attendiTono`). Risultato: **ogni `waitForTimeout` che stava PRIMA di una conversione veniva promosso da navigazione a guardia**. Il conto diceva **79 dove il vero era 56**, e i numeri con cui si era pianificata l'intera giornata (154, 108, 90) erano gonfiati — *il 186 del 14a invece è giusto, nessuna di quelle funzioni esisteva ancora*. È la regola 37 applicata allo strumento di misura: **una misura che non misura, e non lo dice.** Corretto leggendo `module.exports` di `attese.js` invece di elencarli — una funzione nuova è riconosciuta il giorno stesso. `test_conta_attese.js` ha ora un blocco **generato dagli export**, uno per funzione: con l'elenco a mano rimesso al suo posto il test va rosso (3 guardie diventano 8). | titolo: `14b ④: la famiglia del localStorage, e lo strumento che contava male` |
| 2026-09-11 | **`test_voicecoach.js`: un'etichetta che prometteva più di quanto l'asserzione controllasse.** La riga diceva *«Clicking it marks voiceCoach completed **and returns to the map**»*, ma l'asserzione leggeva **solo** il completamento: il ritorno alla mappa era dichiarato e non verificato da nessuno. Chi leggeva il nome del test credeva che quel comportamento fosse protetto — è la regola 32 vista da un'altra porta. Spezzata in due asserzioni: ora il ritorno alla mappa è **provato**, non dichiarato. *Trovato leggendo il codice per il triage della famiglia ④, non prodotto dalla conversione.* | titolo: `14b ④: la famiglia del localStorage, e lo strumento che contava male` |
| 2026-09-10 | **`tests/tools/attendi.sh` non si arrendeva davvero: aveva un tetto solo, tarato a 90 minuti su una suite di 17, e quando parlava dava l'informazione sbagliata.** Su un lavoro morto diceva «tempo scaduto» — cioè *«ho aspettato troppo»* invece di *«ho aspettato un cadavere»*. Ora ha **due rilevatori**, perché sono due guasti: il **tetto** vede il lavoro vivo che non finisce (uscita 2), il **silenzio sul log** vede il lavoro morto (uscita 3, con due frasi distinte per «ha scritto e poi ha smesso» e «non ha mai scritto niente — controlla il percorso»). Gli argomenti sbagliati escono con **64** e non più con lo stesso 2 del tempo scaduto. La soglia di silenzio (600 s) viene da una misura, non da un'intuizione: la forma di `run_full_regression.sh` rende il silenzio massimo legittimo pari alla durata del file più lento — **192 s** il 2026-09-10, su 1019 s di suite. | titolo: `attendi.sh si arrende da sola, e dice quale dei due guasti ha davanti` |
| 2026-09-08 | **I diciannove punti di `test_batch19.js` erano una specie, non un esemplare.** Delle 28 attese fisse del file, 19 facevano da guardia a un'asserzione, e il rosso in CI cadeva su una riga diversa a ogni corsa perché **il test non controllava lo stato che misurava**. Riscritto **tutto il gruppo** — `[QM Task1]`, `[SR Task1]`, `[SR Task3]`, `[SR Task3-adj]` — con tre funzioni condivise (`statoQuiz`, `toccaFinoA`, `attendiDomandaSuccessiva`) che servono sia Match Practice sia Speed Match: un posto solo da correggere invece di quattro. Il file è sceso da 28 attese a 17. *(La riga stava in `decisioni.md` con la condizione «alla prossima apertura di quel file, e si riscrive tutto il gruppo»: eseguita dal passo 0a-bis, chiusa qui l'11 settembre — era rimasta aperta a torto.)* | `31d6586` e precedenti (passo 0a-bis) |
| 2026-09-07 | `test_batch19.js`: i quattro cicli che cliccano sempre la prima opzione — uno in Match Practice, tre in Speed Match — provavano **venti** giri su un grado che ha **nove** domande. Se la fortuna andava storta per tutte e nove, il giro finiva, le opzioni sparivano, e il click successivo restava appeso trenta secondi su una schermata di riepilogo prima di morire con un `TimeoutError` che non diceva niente. Ora `tapPrimaOpzione()` si accorge che non c'è più niente da toccare e il ciclo esce: l'asserzione «Managed to observe...» subito dopo diventa il rosso che **spiega**. | titolo: `Il ciclo di Speed Match sapeva contare fino a venti su nove domande` |

## Regole di lavoro (`CLAUDE.md`)

| 2026-09-11 | **Regola 44 — l'effetto su cui aspetti non può essere quello che l'asserzione legge.** Nata da due famiglie dello stesso giorno: il suono (aspettare lo spegnimento avrebbe trasformato *«il tocco l'ha fermato»* in *«prima o poi ha smesso»*, vera sempre) e il `localStorage` (aspettare `outcome-verde` avrebbe reso vera per costruzione l'asserzione che quella classe la verifica). Il criterio stava già in testa a `tests/attese.js`, ma **là lo trova chi converte, non chi scrive un test nuovo** — e il difetto nasce lì con la stessa facilità. Porta con sé il rovescio operativo (l'approdo è l'ULTIMO effetto del gesto, e dev'essere uno che nessuna asserzione di quel test legge) e il caso in cui l'asserzione va **spezzata**. | titolo: `Regola 44, e la riga accanto al conto delle attese` |


*Le regole nascono quasi sempre da un difetto vissuto, quindi sono correzioni
come le altre — solo che quello che correggono è **come si lavora**, non il
codice. Stanno qui perché `decisioni.md` deve svuotarsi.*

| Data | Cosa | Commit |
|---|---|---|
| 2026-09-11 | **Regola 43 — ogni commit che non è un passo della catena scrive la propria riga, nello stesso commit**, e la verifica è sul **diff**, non sul messaggio: *un commit che tocca codice o test e non tocca un registro è un commit che non ha registrato niente*. Nasce dai quindici commit del 2026-09-10: `decisioni.md` toccato **12 volte su 15**, `correzioni.md` **una**. Il buco non era «registrare» ma registrare **quello che era stato corretto**, e il giorno dopo è servito un giro di allineamento. ⚠️ La prima forma del controllo guardava il messaggio e **dava otto falsi allarmi su quindici** — misurata, non intuita, e scartata per questo. | titolo: `Regola 43: la riga nasce col commit, e la verifica è sul diff` |
| 2026-09-08 | **Regola 40 — un lavoro a più passi sta sempre nella lista attività**, un elemento per passo, col progresso vero (`Suite 15/42`) aggiornato ogni paio di minuti. *Il pallino che gira è un'animazione, non una misura: da solo non distingue «sta lavorando» da «si è fermato».* È la prima cosa del progetto che chi guida può verificare **senza chiedermela**. *(La riga stava in `decisioni.md` con la condizione «va in `CLAUDE.md` come regola permanente»: eseguita, e chiusa qui l'11 settembre — era rimasta aperta a torto.)* | `b314924` *(entrata insieme al passo 6)* |

## Contenuto dell'episodio

| Data | Cosa | Commit |
|---|---|---|
| 2026-09-07 | `data/inglese/it/inglese-it-gate.json` riscritto da `docs/inglese/it/inglese-it-gate.md`: i numeri dichiarati nella fonte (15 voci in A, 7 in B, 9 in C, 9 battute in D, 8 skill, 8 slot) non corrispondevano al file dati (16, 7, 10, 9), quindi l'app mostrava un contenuto diverso da quello deciso. Il grado C non ricopia più le battute a mano: dove la fonte scrive `= dN` la frase viene dalla battuta, così non possono divergere. `episodeTitle` da "Presentarsi" ad "Al gate", ed etichetta del personaggio da "Guida" a "Hostess al gate". | titolo: `Il file episodio riscritto dalla sua fonte` |

## Why We Say It

| Data | Cosa | Commit |
|---|---|---|
| 2026-09-04 | Dodici correzioni in un giro: traduzione dentro la bolla e sempre visibile (via il link "Mostra traduzione"); Blocco Ascolto dentro la bolla; bordo e sfondo delle card più marcati; nessuno dei tre pulsanti preselezionato; il pulsante scelto blu accento e non verde; la risposta si può cambiare anche al primo giro; via l'etichetta accanto alla spunta ("✓ NON CHIARA"); le card senza regola prendono la spunta quando la sequenza le supera; venti frasi di supporto per risposta, mai ripetute dentro lo stesso modulo. | `b3f9a4a` |
| 2026-09-04 | A 320 e 360 px "Esci e riprendi dopo" usciva dallo schermo (`.se-complete-row .btn`: ora va a capo dentro il pulsante invece di allargarlo). | `b3f9a4a` |

| 2026-09-07 | Sblocco Sequenziale, variante per dichiarazione: una card `is-ahead` non risponde più a niente. Prima l'attenuazione era solo grafica e il Blocco Ascolto dentro la card restava cliccabile — l'audio partiva da una card futura, aggirando la sequenza che il meccanismo esiste per imporre. Guardia sola in cima al gestore, stessa forma della variante per ascolto nel Dialogo. | `86e7462` |

## Dialogue (Repeat in Time, Real Dialogue)

| Data | Cosa | Commit |
|---|---|---|
| 2026-09-04 | L'area sensibile per passare alla battuta successiva mentre scorre la barra è **tutta la bolla**, non la sola barra del tempo. Vale per entrambi i profili. Il pulsante "Prossima frase" resta. | `b3f9a4a` |
| 2026-09-04 | La Spiegazione di entrambi i moduli dice che si può anche toccare la battuta per andare avanti. | `b3f9a4a` |

| 2026-09-07 | Real Dialogue: toccare una battuta mentre parla ne salta l'audio, come negli altri due profili. Il salto stava **dopo** la guardia `advance === 'auto'`, che usciva prima: una perdita causata dall'ordine delle righe, non una scelta. | `86e7462` |

## Voice Practice / Voice Check

| Data | Cosa | Commit |
|---|---|---|
| 2026-09-07 | Premere il microfono e non parlare ora **conta** come registrazione senza parole: il ramo del timeout di silenzio incrementa `vcEmptyRecognitionStreak` e aggiorna l'avviso. Prima quel contatore si muoveva solo dentro `vcEvaluate()`, cioè dopo un "Invia" — e una registrazione muta viene scartata prima di arrivarci, quindi il gesto più comune di chi ha il microfono rotto era l'unico che all'avviso non arrivava mai. Non erano due meccanismi in competizione: era uno che non veniva mai innescato. Livello 1 dopo due tentativi. Il pannello è uscito da `#vc-result`, che nello stato di riposo è nascosto: da lì dentro sarebbe salito di livello restando invisibile. Quando l'avviso c'è, la riga sotto il microfono si toglie invece di ripetere lo stesso concetto. | titolo: `Premere e non parlare conta come registrazione vuota` |

## Speed Match

| Data | Cosa | Commit |
|---|---|---|
| 2026-09-05 | Tolto `CONFIG.speedRound.pointsPerCorrect`: parametro presente nel Pannello Admin e mai letto da nessuno — una manopola che non muoveva niente (decisione D4). | `4b32777` |
